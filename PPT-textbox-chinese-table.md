# 文本框交互规则表格（完全中文版）

> 本文档提供完全中文化的文本框交互规则表格，所有字段都使用中文，便于中国团队使用

---

## 一、表格结构设计

### 1.1 列结构（8列）

| 列名 | 说明 | 示例值 |
|-----|------|-------|
| **A: 当前状态** | 对象当前所处的状态 | 默认态、悬停态、选中态 |
| **B: 可交互属性** | 用户鼠标操作的区域 | 填充区域、文本内容区、缩放点 |
| **C: 属性可见** | 该属性是否显示 | 是、否 |
| **D: 鼠标样式** | 鼠标悬停时的光标形状 | 移动光标、文本光标、缩放光标 |
| **E: 允许的操作** | 用户可执行的键鼠操作 | 单击、双击、拖拽 |
| **F: 目标状态** | 执行操作后进入的状态 | 选中态、编辑态、移动中 |
| **G: 视觉反馈** | 界面上的视觉变化 | 显示控制点、高亮边框 |
| **H: 备注说明** | 补充说明和约束条件 | 按住Shift等比缩放 |

---

## 二、完整交互规则表格

### 2.1 表头

```
列A        列B          列C      列D        列E          列F        列G          列H
当前状态   可交互属性   属性可见  鼠标样式   允许的操作   目标状态   视觉反馈     备注说明
```

---

### 2.2 完整数据表格（84行）

以下表格可以直接复制到 Excel：

---

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **默认态** | 填充区域 | 是 | 默认光标 | 无 | - | 无 | 对象在默认态不响应任何操作 |
| 默认态 | 文本内容区 | 是 | 默认光标 | 无 | - | 无 | 对象在默认态不响应任何操作 |
| 默认态 | 边框 | 是 | 默认光标 | 无 | - | 无 | 对象在默认态不响应任何操作 |
| 默认态 | 左上角缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 右上角缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 右下角缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 左下角缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 上边缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 右边缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 下边缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 左边缩放点 | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| 默认态 | 旋转柄 | 否 | - | - | - | 不显示 | 旋转柄仅在选中态显示 |
| | | | | | | | |
| **悬停态** | 填充区域 | 是 | 移动光标（四向箭头） | 单击 / 拖拽 | 选中态 / 移动中 | 可选：轻微高亮边框 | 单击选中对象，拖拽直接移动 |
| 悬停态 | 文本内容区 | 是 | 文本光标（工字型） | 单击 / 双击 | 选中态 / 编辑态 | 可选：文本区域高亮 | 单击选中对象，双击进入编辑 |
| 悬停态 | 边框 | 是 | 移动光标（四向箭头） | 单击 / 拖拽 | 选中态 / 移动中 | 同填充区域 | 行为与填充区域相同 |
| 悬停态 | 左上角缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 右上角缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 右下角缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 左下角缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 上边缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 右边缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 下边缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 左边缩放点 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| 悬停态 | 旋转柄 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| | | | | | | | |
| **选中态** | 填充区域 | 是 | 移动光标（四向箭头） | 拖拽 / 按ESC键 / 点击外部 | 移动中 / 默认态 / 默认态 | 显示选择框和所有控制点 | 拖拽移动对象；ESC或点击外部取消选中 |
| 选中态 | 文本内容区 | 是 | 文本光标（工字型） | 双击 / 按F2键 | 编辑态 / 编辑态 | 文本区域可能高亮 | 双击或按F2键进入编辑态 |
| 选中态 | 边框 | 是 | 移动光标（四向箭头） | 拖拽 | 移动中 | 同填充区域 | 行为与填充区域相同 |
| 选中态 | 左上角缩放点 | 是 | 缩放光标（左上-右下） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放；按Shift键等比缩放 |
| 选中态 | 右上角缩放点 | 是 | 缩放光标（右上-左下） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放；按Shift键等比缩放 |
| 选中态 | 右下角缩放点 | 是 | 缩放光标（左上-右下） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放；按Shift键等比缩放 |
| 选中态 | 左下角缩放点 | 是 | 缩放光标（右上-左下） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放；按Shift键等比缩放 |
| 选中态 | 上边缩放点 | 是 | 缩放光标（上下） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸高度 |
| 选中态 | 右边缩放点 | 是 | 缩放光标（左右） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸宽度 |
| 选中态 | 下边缩放点 | 是 | 缩放光标（上下） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸高度 |
| 选中态 | 左边缩放点 | 是 | 缩放光标（左右） | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸宽度 |
| 选中态 | 旋转柄 | 是 | 旋转光标（圆形箭头） | 拖拽 | 旋转中 | 圆形图标，有连接线到对象 | 按Shift键以15°为单位旋转 |
| | | | | | | | |
| **编辑态** | 填充区域 | 是 | 默认光标 | 无 | - | 显示但不响应 | 编辑态时焦点在文本内容区 |
| 编辑态 | 文本内容区 | 是 | 文本光标（工字型） | 文本输入 / 单击 / 拖拽 / 按ESC键 / 点击外部 | 编辑态 / 编辑态 / 编辑态 / 选中态 / 选中态 | 显示闪烁的文本光标；选中文本时蓝色背景 | 单击移动光标；拖拽选择文本；ESC键退出编辑 |
| 编辑态 | 边框 | 是 | - | - | - | 显示为虚线 | 仅显示，不可交互 |
| 编辑态 | 左上角缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点，防止误操作 |
| 编辑态 | 右上角缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点 |
| 编辑态 | 右下角缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点 |
| 编辑态 | 左下角缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点 |
| 编辑态 | 上边缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点 |
| 编辑态 | 右边缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点 |
| 编辑态 | 下边缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点 |
| 编辑态 | 左边缩放点 | 否 | - | - | - | 隐藏 | 编辑态隐藏所有控制点 |
| 编辑态 | 旋转柄 | 否 | - | - | - | 隐藏 | 编辑态隐藏旋转柄 |
| | | | | | | | |
| **移动中** | 填充区域 | 是 | 移动光标（四向箭头） | 拖拽中 / 释放鼠标 / 按ESC键 | 移动中 / 选中态 / 选中态 | 对象跟随鼠标移动；显示对齐辅助线 | 持续拖拽保持移动；释放鼠标或ESC键完成 |
| 移动中 | 文本内容区 | 是 | - | - | - | 随对象移动 | 文本随对象移动，但不响应交互 |
| 移动中 | 边框 | 是 | - | - | - | 随对象移动 | 边框随对象移动 |
| 移动中 | 左上角缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 右上角缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 右下角缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 左下角缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 上边缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 右边缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 下边缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 左边缩放点 | 否 | - | - | - | 不显示 | 移动中隐藏所有控制点 |
| 移动中 | 旋转柄 | 否 | - | - | - | 不显示 | 移动中隐藏旋转柄 |
| | | | | | | | |
| **缩放中** | 填充区域 | 是 | - | - | - | 随对象缩放 | 填充区域随对象缩放，但不响应交互 |
| 缩放中 | 文本内容区 | 是 | - | - | - | 随对象缩放 | 文本随对象缩放 |
| 缩放中 | 边框 | 是 | - | - | - | 随对象缩放 | 边框随对象缩放 |
| 缩放中 | 左上角缩放点 | 是 | 缩放光标（左上-右下） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示（如300x200） | 持续拖拽缩放；释放鼠标或ESC键完成 |
| 缩放中 | 右上角缩放点 | 是 | 缩放光标（右上-左下） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示 | 持续拖拽缩放 |
| 缩放中 | 右下角缩放点 | 是 | 缩放光标（左上-右下） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示 | 持续拖拽缩放 |
| 缩放中 | 左下角缩放点 | 是 | 缩放光标（右上-左下） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示 | 持续拖拽缩放 |
| 缩放中 | 上边缩放点 | 是 | 缩放光标（上下） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示 | 单向拉伸高度 |
| 缩放中 | 右边缩放点 | 是 | 缩放光标（左右） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示 | 单向拉伸宽度 |
| 缩放中 | 下边缩放点 | 是 | 缩放光标（上下） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示 | 单向拉伸高度 |
| 缩放中 | 左边缩放点 | 是 | 缩放光标（左右） | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | 实时显示尺寸提示 | 单向拉伸宽度 |
| 缩放中 | 旋转柄 | 否 | - | - | - | 不显示 | 缩放中隐藏旋转柄 |
| | | | | | | | |
| **旋转中** | 填充区域 | 是 | - | - | - | 随对象旋转 | 填充区域随对象旋转，但不响应交互 |
| 旋转中 | 文本内容区 | 是 | - | - | - | 随对象旋转 | 文本随对象旋转 |
| 旋转中 | 边框 | 是 | - | - | - | 随对象旋转 | 边框随对象旋转 |
| 旋转中 | 左上角缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 右上角缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 右下角缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 左下角缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 上边缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 右边缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 下边缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 左边缩放点 | 否 | - | - | - | 不显示 | 旋转中隐藏所有缩放点 |
| 旋转中 | 旋转柄 | 是 | 旋转光标（圆形箭头） | 拖拽中 / 释放鼠标 / 按ESC键 | 旋转中 / 选中态 / 选中态 | 实时显示角度提示（如45°） | 持续拖拽旋转；释放鼠标或ESC键完成 |

---

## 三、术语对照表

### 3.1 状态术语对照

| 中文 | 英文 | 说明 |
|-----|------|------|
| 默认态 | Idle | 对象创建后未交互的初始状态 |
| 悬停态 | Hovered | 鼠标悬停在对象上但未点击 |
| 选中态 | Selected | 对象被选中，显示控制点 |
| 编辑态 | Editing | 正在编辑文本内容 |
| 移动中 | Moving | 正在拖拽移动对象 |
| 缩放中 | Resizing | 正在缩放对象尺寸 |
| 旋转中 | Rotating | 正在旋转对象角度 |

### 3.2 属性术语对照

| 中文 | 英文 | 说明 |
|-----|------|------|
| 填充区域 | Fill Area | 文本框的主体区域 |
| 文本内容区 | Text Content Area | 包含文字的区域 |
| 边框 | Border | 文本框的外边框线 |
| 左上角缩放点 | Top-Left Resize Handle | 左上角的缩放控制点 |
| 右上角缩放点 | Top-Right Resize Handle | 右上角的缩放控制点 |
| 右下角缩放点 | Bottom-Right Resize Handle | 右下角的缩放控制点 |
| 左下角缩放点 | Bottom-Left Resize Handle | 左下角的缩放控制点 |
| 上边缩放点 | Top Edge Resize Handle | 上边中点的缩放控制点 |
| 右边缩放点 | Right Edge Resize Handle | 右边中点的缩放控制点 |
| 下边缩放点 | Bottom Edge Resize Handle | 下边中点的缩放控制点 |
| 左边缩放点 | Left Edge Resize Handle | 左边中点的缩放控制点 |
| 旋转柄 | Rotation Handle | 顶部的旋转控制手柄 |

### 3.3 操作术语对照

| 中文 | 英文 | 说明 |
|-----|------|------|
| 单击 | Click | 鼠标左键单击一次 |
| 双击 | Double Click | 鼠标左键快速点击两次 |
| 拖拽 | Drag | 按住鼠标左键并移动 |
| 拖拽中 | Dragging | 持续拖拽的过程 |
| 释放鼠标 | Mouse Up | 松开鼠标左键 |
| 按ESC键 | Press ESC | 按下键盘ESC键 |
| 按F2键 | Press F2 | 按下键盘F2键 |
| 点击外部 | Click Outside | 点击对象外部区域 |
| 文本输入 | Text Input | 键盘输入文字 |

### 3.4 光标样式术语对照

| 中文 | 英文 | 图标描述 |
|-----|------|---------|
| 默认光标 | Default | 普通箭头指针 |
| 移动光标（四向箭头） | Move | ↕↔ 四个方向的箭头 |
| 文本光标（工字型） | I-beam / Text | &#124; 工字形光标 |
| 缩放光标（左上-右下） | NWSE-Resize | ↖↘ 斜向双箭头 |
| 缩放光标（右上-左下） | NESW-Resize | ↗↙ 斜向双箭头 |
| 缩放光标（上下） | NS-Resize | ↕ 上下双箭头 |
| 缩放光标（左右） | EW-Resize | ↔ 左右双箭头 |
| 旋转光标（圆形箭头） | Rotate / Alias | 🔄 圆形旋转箭头 |

---

## 四、补充表格

### 4.1 状态转换汇总表

| 起始状态 | 目标状态 | 触发条件 | 涉及属性 | 说明 |
|---------|---------|---------|---------|------|
| 默认态 | 悬停态 | 鼠标进入 | 任意属性 | 鼠标进入对象区域 |
| 悬停态 | 默认态 | 鼠标离开 | - | 鼠标离开对象区域 |
| 悬停态 | 选中态 | 单击 | 填充区域 / 文本内容区 | 单击选中对象 |
| 悬停态 | 编辑态 | 双击 | 文本内容区 | 双击文本直接进入编辑 |
| 悬停态 | 移动中 | 拖拽 | 填充区域 | 拖拽直接进入移动 |
| 选中态 | 默认态 | 点击外部 / 按ESC键 | - | 取消选中 |
| 选中态 | 编辑态 | 双击 / 按F2键 | 文本内容区 | 进入文本编辑 |
| 选中态 | 移动中 | 拖拽 | 填充区域 | 拖拽主体移动 |
| 选中态 | 缩放中 | 拖拽 | 缩放点（8个） | 拖拽缩放点缩放 |
| 选中态 | 旋转中 | 拖拽 | 旋转柄 | 拖拽旋转柄旋转 |
| 编辑态 | 选中态 | 按ESC键 / 点击外部 | - | 退出编辑，返回选中态 |
| 移动中 | 选中态 | 释放鼠标 / 按ESC键 | - | 完成移动或取消 |
| 缩放中 | 选中态 | 释放鼠标 / 按ESC键 | - | 完成缩放或取消 |
| 旋转中 | 选中态 | 释放鼠标 / 按ESC键 | - | 完成旋转或取消 |

### 4.2 属性检测优先级表

| 优先级 | 属性类型 | 包含的属性 | 检测范围 | 说明 |
|-------|---------|----------|---------|------|
| **1（最高）** | 控制点 | 8个缩放点、旋转柄 | 热区半径 6-8像素 | 优先响应控制点 |
| **2** | 特殊交互区 | 文本内容区 | 文本字符边界 | 精确检测文本区域 |
| **3** | 边界 | 边框 | 边框宽度 ±3像素 | 边框附近区域 |
| **4（最低）** | 主体 | 填充区域 | 对象内部全部区域 | 兜底响应 |

**检测逻辑说明：**
1. 当对象处于"选中态"时，优先检测鼠标是否在控制点的热区范围内
2. 如果不在控制点范围内，检测是否在文本字符边界内
3. 如果不在文本区域内，检测是否在边框附近（±3像素）
4. 最后检测是否在填充区域内

### 4.3 状态详细定义表

| 状态名称 | 描述 | 进入条件 | 退出条件 | 主要特征 |
|---------|------|---------|---------|---------|
| 默认态 | 对象创建后未交互的初始状态 | 对象创建；取消选中 | 鼠标进入；点击对象 | 无控制点；无交互反馈 |
| 悬停态 | 鼠标悬停在对象上但未点击 | 鼠标进入对象区域 | 鼠标离开；点击 | 光标变化；可选高亮 |
| 选中态 | 对象被选中，显示控制点 | 点击对象 | 点击外部；按ESC键 | 显示8个缩放点和旋转柄 |
| 编辑态 | 正在编辑文本内容 | 双击文本；按F2键 | 按ESC键；点击外部 | 显示文本光标；隐藏控制点 |
| 移动中 | 正在拖拽移动对象 | 拖拽对象主体 | 释放鼠标；按ESC键 | 对象跟随鼠标；对齐辅助线 |
| 缩放中 | 正在缩放对象尺寸 | 拖拽缩放点 | 释放鼠标；按ESC键 | 实时缩放；显示尺寸提示 |
| 旋转中 | 正在旋转对象角度 | 拖拽旋转柄 | 释放鼠标；按ESC键 | 实时旋转；显示角度提示 |

### 4.4 属性详细定义表

| 属性名称 | 几何类型 | 描述 | 适用状态 | 检测范围 | 备注 |
|---------|---------|------|---------|---------|------|
| 填充区域 | 区域 | 文本框的主体区域，不包括控制点 | 所有状态 | 对象边界内的全部区域 | 最低优先级，兜底响应 |
| 文本内容区 | 区域 | 包含文字的区域 | 所有状态 | 文本字符的边界框 | 精确检测文本边界 |
| 边框 | 线 | 文本框的外边框线 | 所有状态 | 边框宽度 ±3像素 | 行为通常与填充区相同 |
| 左上角缩放点 | 点 | 左上角的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：左上-右下双箭头 |
| 右上角缩放点 | 点 | 右上角的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：右上-左下双箭头 |
| 右下角缩放点 | 点 | 右下角的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：左上-右下双箭头 |
| 左下角缩放点 | 点 | 左下角的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：右上-左下双箭头 |
| 上边缩放点 | 点 | 上边中点的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：上下双箭头；单向拉伸高度 |
| 右边缩放点 | 点 | 右边中点的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：左右双箭头；单向拉伸宽度 |
| 下边缩放点 | 点 | 下边中点的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：上下双箭头；单向拉伸高度 |
| 左边缩放点 | 点 | 左边中点的缩放控制点 | 仅选中态、缩放中 | 热区半径 6像素 | 光标：左右双箭头；单向拉伸宽度 |
| 旋转柄 | 点 | 顶部的旋转控制手柄 | 仅选中态、旋转中 | 热区半径 8像素 | 光标：圆形旋转箭头 |

---

## 五、Excel 工作簿结构建议

### 工作簿名称
```
文本框交互规则表格（完全中文版）.xlsx
```

### Sheet 结构

```
├─ Sheet 1: 【交互规则总表】
│    ↑ 主表格，包含所有 84 行规则
│
├─ Sheet 2: 【状态转换汇总】
│    ↑ 状态转换的快速查询表
│
├─ Sheet 3: 【属性检测优先级】
│    ↑ 属性检测的优先级规则
│
├─ Sheet 4: 【状态详细定义】
│    ↑ 7个状态的详细说明
│
├─ Sheet 5: 【属性详细定义】
│    ↑ 12个属性的详细说明
│
└─ Sheet 6: 【术语对照表】
     ↑ 中英文术语对照
```

---

## 六、格式美化建议

### 6.1 列宽设置

- A列（当前状态）：100px
- B列（可交互属性）：120px
- C列（属性可见）：80px
- D列（鼠标样式）：140px
- E列（允许的操作）：180px
- F列（目标状态）：100px
- G列（视觉反馈）：200px
- H列（备注说明）：250px

### 6.2 颜色方案

**表头行（第1行）：**
- 背景色：深蓝色（RGB: 68, 114, 196）
- 字体：白色，加粗，12号字
- 对齐：居中

**状态分组标题行：**
- 背景色：浅灰色（RGB: 217, 217, 217）
- 字体：黑色，加粗，11号字
- 边框：上边框加粗

**数据行：**
- 交替行颜色：
  - 白色（RGB: 255, 255, 255）
  - 浅蓝色（RGB: 221, 235, 247）
- 字体：黑色，常规，11号字
- 对齐：左对齐

**"属性可见"列：**
- "是"：绿色字体（RGB: 0, 176, 80）
- "否"：灰色字体（RGB: 128, 128, 128）

**"目标状态"列：**
- 有转换的单元格：浅黄色背景（RGB: 255, 242, 204）
- 无转换的单元格：默认背景

### 6.3 边框设置

- 所有单元格：细边框（灰色）
- 状态分组行：上边框加粗（黑色）
- 表头行：全边框加粗（白色）

---

## 七、使用说明

### 7.1 筛选功能

**按当前状态筛选：**
- 选中表头行 → 数据 → 筛选
- 点击"当前状态"列的下拉箭头
- 选择要查看的状态（如：选中态）

**按属性筛选：**
- 点击"可交互属性"列的下拉箭头
- 选择要查看的属性（如：文本内容区）

**按操作筛选：**
- 点击"允许的操作"列的下拉箭头
- 选择要查看的操作（如：拖拽）

### 7.2 分组功能

**按状态分组：**
1. 选中数据区域
2. 数据 → 分级显示 → 创建组
3. 每个状态的数据作为一组
4. 可以折叠/展开各状态

### 7.3 搜索功能

**快速查找：**
1. 按 Ctrl+F 打开查找对话框
2. 输入关键词（如："双击"、"旋转"、"编辑态"）
3. 点击"查找全部"查看所有匹配项

---

## 八、使用场景示例

### 8.1 产品经理使用

**需求文档引用：**
```
需求：文本框的选中和编辑交互

规则详见《文本框交互规则表格（完全中文版）.xlsx》：
- 选中规则：Sheet1 第26-37行
- 编辑规则：Sheet1 第38-49行
- 状态转换：Sheet2 第6-7行
```

### 8.2 UI/UX 设计师使用

**交互设计参考：**
```
场景：用户悬停在文本框的文本内容区

查询方法：
1. 打开 Sheet1
2. 筛选"当前状态" = "悬停态"
3. 筛选"可交互属性" = "文本内容区"
4. 查看结果：第14行

设计要点：
- 鼠标样式：文本光标（工字型）
- 允许的操作：单击 / 双击
- 视觉反馈：可选的文本区域高亮
```

### 8.3 开发工程师使用

**编码实现参考：**
```javascript
// 根据表格 Sheet1 第14行实现
if (currentState === "悬停态" &&
    property === "文本内容区") {
    setCursor("文本光标");

    on单击 = () => {
        setState("选中态");
    };

    on双击 = () => {
        setState("编辑态");
    };
}
```

### 8.4 测试工程师使用

**测试用例生成：**
```
测试用例 TC-014
标题：悬停态下双击文本内容区进入编辑态

前置条件：
- 文本框处于"悬停态"
- 鼠标悬停在"文本内容区"

测试步骤：
1. 鼠标悬停在文本内容区
2. 执行双击操作

预期结果：
- 鼠标样式：文本光标（工字型）
- 状态转换：悬停态 → 编辑态
- 视觉反馈：显示闪烁的文本光标

依据：Sheet1 第14行
```

---

## 九、总结

### 9.1 完全中文化的优势

✅ **易读性**
- 所有术语都使用中文，无需查询对照表
- 适合中国团队直接使用

✅ **易沟通**
- 产品、设计、开发、测试都用同一套中文术语
- 减少沟通成本和理解偏差

✅ **易维护**
- 中文描述更直观，便于后续修改和扩展
- 新成员更容易理解和上手

✅ **易应用**
- 可以直接复制到需求文档、设计稿、代码注释
- 无需二次翻译

### 9.2 表格数据统计

- **总行数**：84 行（7个状态 × 12个属性）
- **总列数**：8 列
- **状态转换规则**：13 条（见状态转换汇总表）
- **可交互属性**：12 个
- **交互状态**：7 个
- **光标样式**：8 种

---

**这个完全中文化的表格可以直接复制到 Excel 使用！**

**所有英文缩写（如 S0, S1, P01）和英文术语（如 Click, Drag, I-beam）都已替换为中文。**
