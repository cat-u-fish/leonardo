# 文本框交互规则分层架构实施方案

> 本文档提供"分层架构 + 渐进实施 + 规则优先级"的完整实施方案

---

## 一、分层架构设计

### 1.1 三层架构总览

```
┌─────────────────────────────────────────────┐
│           第3层：约束规则层                   │
│   - 优先级：最高（Level 4）                  │
│   - 作用：强制覆盖下层所有规则                │
│   - 示例：锁定、成组、母版对象                │
└─────────────────────────────────────────────┘
                      ↓ 覆盖
┌─────────────────────────────────────────────┐
│           第2层：平台差异层                   │
│   - 优先级：高（Level 3）                    │
│   - 作用：覆盖基础规则的平台特定行为           │
│   - 示例：Mac单击直接编辑、Windows两步编辑    │
└─────────────────────────────────────────────┘
                      ↓ 覆盖
┌─────────────────────────────────────────────┐
│           第1.5层：修饰键规则层               │
│   - 优先级：中（Level 2）                    │
│   - 作用：修改基础规则的行为                  │
│   - 示例：Shift约束方向、Option复制           │
└─────────────────────────────────────────────┘
                      ↓ 修饰
┌─────────────────────────────────────────────┐
│           第1层：基础规则层                   │
│   - 优先级：基础（Level 1）                  │
│   - 作用：定义标准场景的核心行为              │
│   - 示例：拖拽移动、双击编辑                  │
└─────────────────────────────────────────────┘
```

### 1.2 规则优先级定义

| 层级 | 名称 | 优先级 | 作用方式 | 说明 |
|-----|------|-------|---------|------|
| **第3层** | 约束规则层 | Level 4（最高） | **覆盖（Override）** | 强制覆盖所有下层规则，不可被修改 |
| **第2层** | 平台差异层 | Level 3（高） | **覆盖（Override）** | 覆盖基础规则，提供平台特定行为 |
| **第1.5层** | 修饰键规则层 | Level 2（中） | **修饰（Modify）** | 在基础规则上叠加效果 |
| **第1层** | 基础规则层 | Level 1（基础） | **定义（Define）** | 定义默认行为 |

### 1.3 规则计算逻辑

```typescript
function calculateBehavior(
  property: string,      // 可交互属性
  state: string,         // 当前状态
  platform: string,      // 平台（Windows/Mac）
  modifierKeys: string[],// 修饰键（Shift, Option等）
  constraints: string[]  // 约束条件（已锁定、已成组等）
): Behavior {

  // 步骤1：查询基础规则（Level 1）
  let behavior = queryBaseRule(property, state);

  // 步骤2：应用修饰键规则（Level 2）
  if (modifierKeys.length > 0) {
    const modifierRule = queryModifierRule(property, state, modifierKeys);
    if (modifierRule) {
      behavior = applyModifier(behavior, modifierRule);
    }
  }

  // 步骤3：应用平台差异规则（Level 3）
  const platformRule = queryPlatformRule(property, state, platform);
  if (platformRule) {
    behavior = platformRule; // 完全覆盖
  }

  // 步骤4：应用约束规则（Level 4，最高优先级）
  for (const constraint of constraints) {
    const constraintRule = queryConstraintRule(property, state, constraint);
    if (constraintRule) {
      behavior = constraintRule; // 强制覆盖
    }
  }

  return behavior;
}
```

---

## 二、Excel 表格结构设计

### 2.1 完整列结构（12列）

| 列 | 列名 | 数据类型 | 说明 | 示例值 |
|---|------|---------|------|-------|
| **A** | 规则ID | 文本 | 唯一标识符 | R001, R002 |
| **B** | 规则层级 | 枚举 | Level 1/2/3/4 | Level 1（基础） |
| **C** | 当前状态 | 枚举 | 对象状态 | 选中态 |
| **D** | 可交互属性 | 枚举 | 属性 | 填充区域 |
| **E** | 平台 | 枚举 | 通用/Mac/Windows | 通用 |
| **F** | 修饰键 | 文本 | 按住的修饰键 | 无、Shift、Option、Shift+Option |
| **G** | 约束条件 | 文本 | 对象约束 | 无约束、已锁定、已成组 |
| **H** | 鼠标样式 | 文本 | 光标样式 | 移动光标 |
| **I** | 允许的操作 | 文本 | 用户操作 | 拖拽 |
| **J** | 目标状态 | 枚举 | 转换后状态 | 移动中 |
| **K** | 视觉反馈 | 文本 | UI变化 | 对象跟随鼠标移动 |
| **L** | 备注说明 | 文本 | 补充说明 | 标准移动行为 |

**关键改进：**
- ✅ 增加"规则ID"列：便于引用和追踪
- ✅ 增加"规则层级"列：明确规则优先级
- ✅ "平台"列：支持平台差异
- ✅ "修饰键"列：支持修饰键规则
- ✅ "约束条件"列：支持约束规则

### 2.2 工作簿结构（8个Sheet）

```
文本框交互规则（分层架构版）.xlsx

├─ Sheet 1: 【总览与说明】
│    ↑ 分层架构说明、使用指南、规则优先级表
│
├─ Sheet 2: 【第1层-基础规则】
│    ↑ 84行基础规则（Level 1）
│
├─ Sheet 3: 【第1.5层-修饰键规则】
│    ↑ 修饰键规则（Level 2）
│
├─ Sheet 4: 【第2层-Mac平台差异】
│    ↑ Mac特有规则（Level 3）
│
├─ Sheet 5: 【第2层-Windows平台差异】
│    ↑ Windows特有规则（Level 3，可选）
│
├─ Sheet 6: 【第3层-成组约束】
│    ↑ 成组对象的约束规则（Level 4）
│
├─ Sheet 7: 【第3层-锁定约束】
│    ↑ 锁定对象的约束规则（Level 4）
│
└─ Sheet 8: 【规则查询助手】
     ↑ 提供规则查询工具（公式或查询界面）
```

---

## 三、渐进实施路线图

### 3.1 实施阶段划分

#### 🎯 阶段0：准备阶段（1天）

**目标：** 搭建表格框架

**任务清单：**
- [x] 创建 Excel 工作簿
- [x] 创建 8 个 Sheet 并命名
- [x] 在 Sheet 1 添加总览说明
- [x] 在 Sheet 2-7 设置表头（12列）
- [x] 设置列宽和格式

**交付物：** 空白但结构完整的 Excel 工作簿

---

#### ✅ 阶段1：基础规则层（已完成）

**目标：** 完成基础规则的录入

**任务清单：**
- [x] 将之前的 84 行基础规则迁移到 Sheet 2
- [x] 为每条规则添加规则ID（R001-R084）
- [x] 设置"规则层级"列为"Level 1（基础）"
- [x] 设置"平台"列为"通用"
- [x] 设置"修饰键"列为"无"
- [x] 设置"约束条件"列为"无约束"

**交付物：** Sheet 2 包含 84 行完整的基础规则

**验证标准：**
- ✅ 每条规则有唯一ID
- ✅ 所有 12 列都有数据
- ✅ 覆盖所有 (属性 × 状态) 组合

---

#### 🔄 阶段2：修饰键规则层（预计3-5天）

**目标：** 添加常用修饰键规则

**任务清单：**
- [ ] 2.1 枚举常用修饰键组合（约10种）
- [ ] 2.2 为每个修饰键定义作用范围
- [ ] 2.3 填写 Sheet 3 的修饰键规则（约50行）
- [ ] 2.4 测试规则优先级逻辑

**子任务2.1：枚举修饰键组合**

| 修饰键 | 平台 | 作用场景 | 数量 |
|-------|------|---------|------|
| Shift | 通用 | 拖拽、缩放、旋转 | ~10行 |
| Option (Mac) / Alt (Win) | 通用 | 拖拽 | ~5行 |
| Control (Mac) / Ctrl (Win) | 通用 | 缩放、旋转 | ~5行 |
| Command (Mac) / Ctrl (Win) | 通用 | 点击多选 | ~3行 |
| Shift + Option | 通用 | 拖拽 | ~5行 |
| Shift + Control | 通用 | 缩放 | ~3行 |
| 其他组合 | 通用 | 特殊场景 | ~10行 |

**交付物：** Sheet 3 包含约 50 行修饰键规则

**验证标准：**
- ✅ 覆盖常用修饰键组合
- ✅ 规则ID格式：M001-M050
- ✅ 规则层级：Level 2（修饰）

---

#### 🍎 阶段3：平台差异层（预计2-3天）

**目标：** 添加 Mac 和 Windows 的差异规则

**任务清单：**
- [ ] 3.1 研究 Mac 平台的差异点（约10-15个）
- [ ] 3.2 填写 Sheet 4 的 Mac 差异规则（约20行）
- [ ] 3.3 研究 Windows 平台的特殊行为（如有）
- [ ] 3.4 填写 Sheet 5 的 Windows 差异规则（可选）
- [ ] 3.5 测试平台规则覆盖基础规则的逻辑

**子任务3.1：Mac 平台差异点**

| 差异点 | Mac 行为 | Windows 行为 | 影响的规则 |
|-------|---------|-------------|----------|
| 单击文本编辑 | 单击直接编辑 | 单击选中→双击编辑 | 默认态→编辑态 |
| 修饰键名称 | Option, Command | Alt, Ctrl | 所有修饰键规则 |
| 右键菜单 | Control+点击 | 右键 | 上下文菜单 |
| ... | ... | ... | ... |

**交付物：** Sheet 4 包含约 20 行 Mac 差异规则

**验证标准：**
- ✅ 规则ID格式：P001-P020
- ✅ 规则层级：Level 3（平台）
- ✅ 平台列：Mac

---

#### 🔒 阶段4：约束规则层 - 成组（预计3-5天）

**目标：** 添加成组对象的约束规则

**任务清单：**
- [ ] 4.1 定义成组后的新状态（组选中态、组内编辑态等）
- [ ] 4.2 枚举成组对象的交互差异（约20个）
- [ ] 4.3 填写 Sheet 6 的成组约束规则（约30行）
- [ ] 4.4 绘制成组状态的状态转换图
- [ ] 4.5 测试约束规则覆盖所有下层规则的逻辑

**子任务4.1：成组新增状态**

| 状态ID | 状态名称 | 说明 |
|-------|---------|------|
| S2a | 组选中态 | 整个组被选中 |
| S2b | 组内对象选中态 | 进入组内编辑模式，单个对象被选中 |
| S3b | 组内对象编辑态 | 在组内编辑某个对象的文本 |

**交付物：** Sheet 6 包含约 30 行成组约束规则

**验证标准：**
- ✅ 规则ID格式：C001-C030
- ✅ 规则层级：Level 4（约束）
- ✅ 约束条件列：已成组

---

#### 🔐 阶段5：约束规则层 - 锁定（预计2天）

**目标：** 添加锁定对象的约束规则

**任务清单：**
- [ ] 5.1 枚举锁定对象禁止的操作（约15个）
- [ ] 5.2 填写 Sheet 7 的锁定约束规则（约20行）
- [ ] 5.3 定义锁定对象的视觉提示（禁止光标等）

**交付物：** Sheet 7 包含约 20 行锁定约束规则

**验证标准：**
- ✅ 规则ID格式：L001-L020
- ✅ 规则层级：Level 4（约束）
- ✅ 约束条件列：已锁定

---

#### 🔍 阶段6：规则查询助手（预计2-3天）

**目标：** 创建规则查询工具

**任务清单：**
- [ ] 6.1 在 Sheet 8 设计查询界面
- [ ] 6.2 使用 Excel 公式实现规则查询逻辑
- [ ] 6.3 添加查询示例和使用说明
- [ ] 6.4 测试各种查询场景

**查询界面设计：**

```
┌────────────────────────────────────────┐
│         规则查询助手                    │
├────────────────────────────────────────┤
│ 请输入查询条件：                        │
│                                        │
│ 当前状态：   [下拉选择]  选中态         │
│ 可交互属性： [下拉选择]  填充区域       │
│ 平台：       [下拉选择]  通用           │
│ 修饰键：     [下拉选择]  Shift          │
│ 约束条件：   [下拉选择]  无约束         │
│                                        │
│         [查询按钮]                      │
├────────────────────────────────────────┤
│ 查询结果：                             │
│                                        │
│ 规则ID：R025                           │
│ 规则层级：Level 2（修饰）               │
│ 鼠标样式：移动光标                      │
│ 允许的操作：拖拽                        │
│ 目标状态：约束移动中                    │
│ 视觉反馈：移动轨迹限制为水平或垂直      │
│ 备注说明：按住Shift限制移动方向         │
└────────────────────────────────────────┘
```

**交付物：** Sheet 8 包含可用的查询工具

---

### 3.2 实施时间表

| 阶段 | 内容 | 预计时间 | 状态 |
|-----|------|---------|------|
| 阶段0 | 准备阶段 | 1天 | ⏳ 待开始 |
| 阶段1 | 基础规则层 | - | ✅ 已完成 |
| 阶段2 | 修饰键规则层 | 3-5天 | ⏳ 待开始 |
| 阶段3 | 平台差异层 | 2-3天 | ⏳ 待开始 |
| 阶段4 | 成组约束 | 3-5天 | ⏳ 待开始 |
| 阶段5 | 锁定约束 | 2天 | ⏳ 待开始 |
| 阶段6 | 规则查询助手 | 2-3天 | ⏳ 待开始 |
| **总计** | | **13-19天** | |

---

## 四、规则示例（完整展示）

### 4.1 第1层：基础规则示例

| 规则ID | 规则层级 | 当前状态 | 可交互属性 | 平台 | 修饰键 | 约束条件 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|-------|---------|---------|-----------|-----|-------|---------|---------|-----------|---------|---------|---------|
| R025 | Level 1（基础） | 选中态 | 填充区域 | 通用 | 无 | 无约束 | 移动光标（四向箭头） | 拖拽 | 移动中 | 对象跟随鼠标移动 | 标准移动行为 |

---

### 4.2 第1.5层：修饰键规则示例

| 规则ID | 规则层级 | 当前状态 | 可交互属性 | 平台 | 修饰键 | 约束条件 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|-------|---------|---------|-----------|-----|-------|---------|---------|-----------|---------|---------|---------|
| M015 | Level 2（修饰） | 选中态 | 填充区域 | 通用 | **Shift** | 无约束 | 移动光标 | 拖拽 | **约束移动中** | 移动轨迹限制为水平或垂直 | 按住Shift限制移动方向 |
| M016 | Level 2（修饰） | 选中态 | 填充区域 | 通用 | **Option** (Mac) / **Alt** (Win) | 无约束 | **复制光标（+号）** | 拖拽 | **复制移动中** | 对象复制并跟随移动 | 复制而非移动 |
| M017 | Level 2（修饰） | 选中态 | 填充区域 | 通用 | **Shift + Option** | 无约束 | 复制光标（+号） | 拖拽 | **约束复制移动中** | 复制+约束方向 | 组合效果 |

**规则计算示例：**
```
场景：用户在"选中态"按住Shift拖拽"填充区域"

步骤1：查询基础规则 R025
  → 目标状态：移动中

步骤2：应用修饰键规则 M015
  → 目标状态：约束移动中（覆盖）

最终行为：进入"约束移动中"状态
```

---

### 4.3 第2层：平台差异规则示例

| 规则ID | 规则层级 | 当前状态 | 可交互属性 | 平台 | 修饰键 | 约束条件 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|-------|---------|---------|-----------|-----|-------|---------|---------|-----------|---------|---------|---------|
| P001 | Level 3（平台） | **默认态** | 文本内容区 | **Mac** | 无 | 无约束 | 文本光标（工字型） | **单击** | **编辑态** | 直接显示文本光标 | **Mac特有：跳过选中态** |

**规则计算示例：**
```
场景：Mac平台，用户在"默认态"单击"文本内容区"

步骤1：查询基础规则（假设是R012）
  → 目标状态：选中态

步骤2：无修饰键，跳过

步骤3：应用平台规则 P001
  → 目标状态：编辑态（完全覆盖）

最终行为：直接进入"编辑态"（跳过选中态）
```

---

### 4.4 第3层：约束规则示例（成组）

| 规则ID | 规则层级 | 当前状态 | 可交互属性 | 平台 | 修饰键 | 约束条件 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|-------|---------|---------|-----------|-----|-------|---------|---------|-----------|---------|---------|---------|
| C001 | Level 4（约束） | 默认态 | 填充区域 | 通用 | 无 | **已成组** | 移动光标 | **单击** | **组选中态** | 选中整个组，显示组边界框 | 单击选中组，非单个对象 |
| C002 | Level 4（约束） | **组选中态** | 填充区域 | 通用 | 无 | **已成组** | 移动光标 | **双击** | **组内对象选中态** | 进入组内编辑模式 | 双击进入组内 |

**规则计算示例：**
```
场景：用户单击一个"已成组"的文本框

步骤1：查询基础规则（假设是R011）
  → 目标状态：选中态

步骤2-3：无修饰键和平台差异，跳过

步骤4：应用约束规则 C001
  → 目标状态：组选中态（强制覆盖）

最终行为：选中整个组，而非单个对象
```

---

### 4.5 第3层：约束规则示例（锁定）

| 规则ID | 规则层级 | 当前状态 | 可交互属性 | 平台 | 修饰键 | 约束条件 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|-------|---------|---------|-----------|-----|-------|---------|---------|-----------|---------|---------|---------|
| L001 | Level 4（约束） | 选中态 | 填充区域 | 通用 | 无 | **已锁定** | **禁止光标（🚫）** | **无** | **选中态** | 无法移动 | 锁定对象不可移动 |
| L002 | Level 4（约束） | 选中态 | 左上角缩放点 | 通用 | 无 | **已锁定** | **禁止光标** | **无** | **选中态** | 控制点不可操作 | 锁定对象不可缩放 |

**规则计算示例：**
```
场景：用户尝试拖拽一个"已锁定"的文本框

步骤1：查询基础规则 R025
  → 目标状态：移动中

步骤2-3：无修饰键和平台差异，跳过

步骤4：应用约束规则 L001
  → 允许的操作：无（强制覆盖）
  → 目标状态：选中态（保持）
  → 鼠标样式：禁止光标

最终行为：无法移动，显示禁止光标
```

---

## 五、规则优先级详解

### 5.1 优先级冲突解决

**规则：** 高优先级规则完全覆盖低优先级规则

```
Level 4（约束） > Level 3（平台） > Level 2（修饰） > Level 1（基础）
```

**示例：同时存在多层规则**

```
场景：Mac平台，用户按住Shift拖拽一个"已锁定"的文本框

查询结果：
- R025 (Level 1)：拖拽 → 移动中
- M015 (Level 2)：Shift+拖拽 → 约束移动中
- P002 (Level 3)：Mac特有行为（假设有）
- L001 (Level 4)：已锁定 → 禁止操作

优先级计算：
Level 4 > Level 3 > Level 2 > Level 1

最终行为：采用 L001 的规则 → 无法移动，显示禁止光标
```

### 5.2 规则查询算法（伪代码）

```python
def query_rule(state, property, platform, modifiers, constraints):
    rules = []

    # 1. 查询基础规则（Level 1）
    base_rule = query_from_sheet2(state, property)
    if base_rule:
        rules.append(base_rule)

    # 2. 查询修饰键规则（Level 2）
    if modifiers:
        modifier_rule = query_from_sheet3(state, property, modifiers)
        if modifier_rule:
            rules.append(modifier_rule)

    # 3. 查询平台规则（Level 3）
    platform_rule = query_from_sheet4_or_5(state, property, platform)
    if platform_rule:
        rules.append(platform_rule)

    # 4. 查询约束规则（Level 4）
    for constraint in constraints:
        constraint_rule = query_from_sheet6_or_7(state, property, constraint)
        if constraint_rule:
            rules.append(constraint_rule)

    # 5. 按优先级排序并返回最高优先级规则
    rules.sort(key=lambda r: r.level, reverse=True)
    return rules[0] if rules else None
```

### 5.3 Excel 公式实现（简化版）

在 Sheet 8（规则查询助手）中，可以使用 VLOOKUP 或 IF 嵌套实现：

```excel
=IF(
  约束条件<>"无约束",
  VLOOKUP(查询条件, Sheet6!A:L, 列号, FALSE),
  IF(
    平台<>"通用",
    VLOOKUP(查询条件, Sheet4!A:L, 列号, FALSE),
    IF(
      修饰键<>"无",
      VLOOKUP(查询条件, Sheet3!A:L, 列号, FALSE),
      VLOOKUP(查询条件, Sheet2!A:L, 列号, FALSE)
    )
  )
)
```

---

## 六、立即开始的行动步骤

### 6.1 第一步：创建表格框架（今天可以完成）

**任务清单：**

- [ ] 1. 创建新的 Excel 工作簿，命名为"文本框交互规则（分层架构版）.xlsx"

- [ ] 2. 创建 8 个 Sheet：
  - [ ] Sheet 1: 总览与说明
  - [ ] Sheet 2: 第1层-基础规则
  - [ ] Sheet 3: 第1.5层-修饰键规则
  - [ ] Sheet 4: 第2层-Mac平台差异
  - [ ] Sheet 5: 第2层-Windows平台差异
  - [ ] Sheet 6: 第3层-成组约束
  - [ ] Sheet 7: 第3层-锁定约束
  - [ ] Sheet 8: 规则查询助手

- [ ] 3. 在每个 Sheet（Sheet 2-7）设置表头（12列）：
  ```
  规则ID | 规则层级 | 当前状态 | 可交互属性 | 平台 | 修饰键 |
  约束条件 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明
  ```

- [ ] 4. 设置列宽：
  - A列（规则ID）：80px
  - B列（规则层级）：120px
  - C列（当前状态）：100px
  - 其他列：参考之前的设置

- [ ] 5. 在 Sheet 1 添加分层架构说明文档（可以复制本文档的内容）

### 6.2 第二步：迁移基础规则（1小时）

**任务清单：**

- [ ] 1. 打开之前的"文本框交互规则（完全中文版）.xlsx"

- [ ] 2. 复制 84 行基础规则到新表格的 Sheet 2

- [ ] 3. 为每行添加规则ID：
  - 公式：`="R" & TEXT(ROW()-1, "000")`
  - 结果：R001, R002, ..., R084

- [ ] 4. 填充"规则层级"列：全部为"Level 1（基础）"

- [ ] 5. 填充"平台"列：全部为"通用"

- [ ] 6. 填充"修饰键"列：全部为"无"

- [ ] 7. 填充"约束条件"列：全部为"无约束"

### 6.3 第三步：开始添加修饰键规则（从简单开始）

**建议：先添加 Shift 键的规则（最常用）**

**任务清单：**

- [ ] 1. 在 Sheet 3 添加以下规则：

| 规则ID | 规则层级 | 当前状态 | 可交互属性 | 平台 | 修饰键 | 约束条件 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|-------|---------|---------|-----------|-----|-------|---------|---------|-----------|---------|---------|---------|
| M001 | Level 2（修饰） | 选中态 | 填充区域 | 通用 | **Shift** | 无约束 | 移动光标 | 拖拽 | 约束移动中 | 移动限制为水平或垂直 | 按住Shift约束方向 |
| M002 | Level 2（修饰） | 选中态 | 左上角缩放点 | 通用 | **Shift** | 无约束 | 缩放光标 | 拖拽 | 等比缩放中 | 保持宽高比缩放 | 按住Shift等比缩放 |
| M003 | Level 2（修饰） | 选中态 | 旋转柄 | 通用 | **Shift** | 无约束 | 旋转光标 | 拖拽 | 15°增量旋转中 | 角度以15°为单位变化 | 按住Shift精确旋转 |

- [ ] 2. 测试：在 Sheet 8 创建简单的查询公式

- [ ] 3. 验证：查询"选中态 + 填充区域 + Shift"，应返回 M001

---

## 七、成功标准

### 7.1 阶段性成功标准

**阶段1完成标准（✅ 已达成）：**
- [x] Sheet 2 包含 84 行完整的基础规则
- [x] 每条规则有唯一ID（R001-R084）
- [x] 所有12列都有数据

**阶段2完成标准：**
- [ ] Sheet 3 包含至少 30 行修饰键规则
- [ ] 覆盖 Shift、Option/Alt、Control/Ctrl 的常用场景
- [ ] 规则ID格式正确（M001-M030）

**阶段3完成标准：**
- [ ] Sheet 4 包含至少 15 行 Mac 差异规则
- [ ] 包含"单击文本直接编辑"等关键差异
- [ ] 规则ID格式正确（P001-P015）

**阶段4-5完成标准：**
- [ ] Sheet 6-7 包含至少 40 行约束规则
- [ ] 覆盖成组、锁定的主要场景
- [ ] 规则ID格式正确（C001-C030, L001-L020）

**阶段6完成标准：**
- [ ] Sheet 8 提供可用的规则查询工具
- [ ] 能正确处理规则优先级
- [ ] 有使用示例和说明

### 7.2 最终成功标准

**完整性：**
- [ ] 5个Sheet（2-7）共包含约 200 条规则
- [ ] 覆盖 80% 的常见交互场景
- [ ] 所有规则格式一致

**可用性：**
- [ ] 任何团队成员可以快速查询规则
- [ ] 规则查询助手易于使用
- [ ] 文档说明清晰完整

**可维护性：**
- [ ] 规则ID体系清晰
- [ ] 新增规则有明确的流程
- [ ] 规则优先级逻辑易于理解

---

## 八、总结

### 🎯 这个方案的核心优势

✅ **分层架构**
- 基础规则（84个）+ 扩展规则（约120个）= 约 200 条规则
- 避免了 19,200 个单元格的复杂度
- 每层规则独立管理，易于维护

✅ **规则优先级**
- 明确的 4 级优先级体系
- 高优先级规则覆盖低优先级
- 冲突解决逻辑清晰

✅ **渐进实施**
- 分 6 个阶段，每个阶段 1-5 天
- 可以随时停止，已完成的部分仍有价值
- 从简单到复杂，风险可控

✅ **可扩展性**
- 新增规则只需添加新行
- 不影响现有规则
- 支持未来的新需求

---

**你现在可以立即开始"第一步：创建表格框架"！**

需要我帮你生成现成的 Excel 模板，还是有其他问题？