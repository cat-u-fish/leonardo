# 文本框交互规则表格（修正版 - 基于实际观察）

> 本文档是基于实际 PPT 行为观察的修正版本
>
> 修正了之前版本中的以下问题：
> 1. 默认态不应有"鼠标样式"（鼠标不在对象上）
> 2. 编辑态时控制点应该是"可见"的
> 3. 缩放中时其他控制点应该"隐藏"
> 4. 缩放时鼠标样式是"十字光标"而非双向箭头
> 5. 对象缩放时会变半透明

---

## 一、表格结构说明

### 1.1 列结构（8列）

| 列名 | 说明 |
|-----|------|
| **当前状态** | 对象当前所处的状态 |
| **可交互属性** | 用户鼠标操作的区域 |
| **属性可见** | 该属性是否显示 |
| **鼠标样式** | 鼠标悬停时的光标形状 |
| **允许的操作** | 用户可执行的键鼠操作 |
| **目标状态** | 执行操作后进入的状态 |
| **视觉反馈** | 界面上的视觉变化 |
| **备注说明** | 补充说明和约束条件 |

---

## 二、完整交互规则表格（修正版）

### 2.1 默认态（对象在画布上，鼠标不在对象区域）

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **默认态** | 对象整体 | 是 | - | 鼠标进入对象区域 | 悬停态 | 无 | 对象显示在画布上，等待鼠标进入 |

**说明：** 默认态时鼠标不在对象上，不存在"鼠标样式"的概念。只有鼠标进入对象区域才会转为悬停态。

---

### 2.2 悬停态（鼠标在对象区域内，但未点击）

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **悬停态** | 填充区域 | 是 | 移动光标（四向箭头） | 单击 / 拖拽 | 选中态 / 移动中 | 可选：轻微高亮边框 | 单击选中对象，拖拽直接移动 |
| 悬停态 | 文本内容区 | 是 | 文本光标（工字型） | 单击 / 双击 | 选中态 / 编辑态 | 可选：文本区域高亮 | 单击选中对象，双击进入编辑 |
| 悬停态 | 边框 | 是 | 移动光标（四向箭头） | 单击 / 拖拽 | 选中态 / 移动中 | 同填充区域 | 行为与填充区域相同 |
| 悬停态 | 缩放点（8个） | 否 | - | - | - | 不显示 | 悬停态不显示控制点 |
| 悬停态 | 旋转柄 | 否 | - | - | - | 不显示 | 悬停态不显示控制点 |

---

### 2.3 选中态（对象被选中，显示控制点）

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **选中态** | 填充区域 | 是 | 移动光标（四向箭头） | 拖拽 / 按ESC键 / 点击外部 | 移动中 / 默认态 / 默认态 | 显示选择框和所有控制点 | 拖拽移动对象；ESC或点击外部取消选中 |
| 选中态 | 文本内容区 | 是 | 文本光标（工字型） | 双击 / 按F2键 | 编辑态 / 编辑态 | 文本区域可能高亮 | 双击或按F2键进入编辑态 |
| 选中态 | 边框 | 是 | 移动光标（四向箭头） | 拖拽 | 移动中 | 同填充区域 | 行为与填充区域相同 |
| 选中态 | 左上角缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放；按Shift键等比缩放 |
| 选中态 | 右上角缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放 |
| 选中态 | 右下角缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放 |
| 选中态 | 左下角缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 拖拽开始缩放 |
| 选中态 | 上边缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸高度 |
| 选中态 | 右边缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸宽度 |
| 选中态 | 下边缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸高度 |
| 选中态 | 左边缩放点 | 是 | 缩放光标 | 拖拽 | 缩放中 | 显示为蓝色圆点或方块 | 单向拉伸宽度 |
| 选中态 | 旋转柄 | 是 | 旋转光标（圆形箭头） | 拖拽 | 旋转中 | 圆形图标，有连接线到对象 | 按Shift键以15°为单位旋转 |

---

### 2.4 编辑态（正在编辑文本内容）

**重要说明：** 根据实际观察，编辑态时所有控制点（8个缩放点+旋转柄）都是**可见**的，并且可以直接操作。

#### 2.4.1 双击文字进入编辑态

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **编辑态** | 填充区域 | 是 | 移动光标（四向箭头） | 拖拽 | 移动中 | 填充区可拖拽移动 | 鼠标移出文本区域时可拖拽移动 |
| 编辑态 | 文本内容区 | 是 | 正常光标 | 双击文字 | 编辑态 | 文字全选，背景变蓝 | **双击文字会全选该文本** |
| 编辑态 | 文本内容区 | 是 | 文本光标（工字型） | 单击 / 文本输入 / 拖拽 / 按ESC键 / 点击外部 | 编辑态 / 编辑态 / 编辑态 / 选中态 / 选中态 | 单击定位光标；输入文字；拖拽选择文本 | **工字型光标热区很小**，移出即变移动光标；ESC键退出编辑 |
| 编辑态 | 边框 | 是 | - | - | - | 显示为虚线 | 仅显示，不可交互 |
| 编辑态 | 左上角缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | **编辑态时控制点可见且可操作** |
| 编辑态 | 右上角缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | 可直接拖拽缩放 |
| 编辑态 | 右下角缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | 可直接拖拽缩放 |
| 编辑态 | 左下角缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | 可直接拖拽缩放 |
| 编辑态 | 上边缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | 可直接拖拽缩放 |
| 编辑态 | 右边缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | 可直接拖拽缩放 |
| 编辑态 | 下边缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | 可直接拖拽缩放 |
| 编辑态 | 左边缩放点 | **是** ✅ | 缩放光标 | 拖拽 | 缩放中 | 显示，可操作 | 可直接拖拽缩放 |
| 编辑态 | 旋转柄 | **是** ✅ | 旋转光标（圆形箭头） | 拖拽 | 旋转中 | 显示，可操作 | 可直接拖拽旋转 |

#### 2.4.2 双击空白区域进入编辑态

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **编辑态** | 文本内容区 | 是 | 文本光标（工字型） | 文本输入 / 单击 / 拖拽 | 编辑态 / 编辑态 / 编辑态 | 光标定位到文字尾部 | **双击空白区域，光标定位到文字尾部** |

**关键发现：**
- 双击**文字**：文字全选，背景变蓝，光标为正常光标
- 双击**空白区域**：光标定位到文字尾部，工字型光标
- 工字型光标的**热区很小**，稍微移出就变成四向移动光标

---

### 2.5 移动中（正在拖拽移动对象）

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **移动中** | 填充区域 | 是 | 移动光标（四向箭头） | 拖拽中 / 释放鼠标 / 按ESC键 | 移动中 / 选中态 / 选中态 | 对象跟随鼠标移动；显示对齐辅助线 | 持续拖拽保持移动；释放鼠标或ESC键完成 |
| 移动中 | 文本内容区 | 是 | - | - | - | 随对象移动 | 文本随对象移动，但不响应交互 |
| 移动中 | 边框 | 是 | - | - | - | 随对象移动 | 边框随对象移动 |
| 移动中 | 左上角缩放点 | **否** ❌ | - | - | - | **隐藏** | **移动中所有控制点隐藏** |
| 移动中 | 右上角缩放点 | **否** ❌ | - | - | - | **隐藏** | 移动中所有控制点隐藏 |
| 移动中 | 右下角缩放点 | **否** ❌ | - | - | - | **隐藏** | 移动中所有控制点隐藏 |
| 移动中 | 左下角缩放点 | **否** ❌ | - | - | - | **隐藏** | 移动中所有控制点隐藏 |
| 移动中 | 上边缩放点 | **否** ❌ | - | - | - | **隐藏** | 移动中所有控制点隐藏 |
| 移动中 | 右边缩放点 | **否** ❌ | - | - | - | **隐藏** | 移动中所有控制点隐藏 |
| 移动中 | 下边缩放点 | **否** ❌ | - | - | - | **隐藏** | 移动中所有控制点隐藏 |
| 移动中 | 左边缩放点 | **否** ❌ | - | - | - | **隐藏** | 移动中所有控制点隐藏 |
| 移动中 | 旋转柄 | **否** ❌ | - | - | - | **隐藏** | 移动中旋转柄隐藏 |

**关键规律：** 移动中只能改变位置，所有控制点消失。

---

### 2.6 缩放中（正在拖拽缩放点）

**重要发现：**
- 鼠标样式是**十字光标** ➕（不是双向箭头）
- 只有**正在操作的缩放点**可见
- **其他7个缩放点**和**旋转柄**全部隐藏
- 对象变为**半透明**
- 触发**对齐提示辅助线**

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **缩放中** | 填充区域 | 是 | - | - | - | **对象变半透明** | 填充区随对象缩放，但不响应交互 |
| 缩放中 | 文本内容区 | 是 | - | - | - | 随对象缩放 | 文本随对象缩放 |
| 缩放中 | 边框 | 是 | - | - | - | 随对象缩放 | 边框随对象缩放 |
| 缩放中 | **正在操作的缩放点** | **是** ✅ | **十字光标** ➕ | 拖拽中 / 释放鼠标 / 按ESC键 | 缩放中 / 选中态 / 选中态 | **实时显示尺寸提示**；触发对齐辅助线 | **只有当前拖拽的点可见**；持续拖拽缩放；释放或ESC完成 |
| 缩放中 | **其他缩放点（7个）** | **否** ❌ | - | - | - | **全部隐藏** | **其他7个缩放点隐藏** |
| 缩放中 | 旋转柄 | **否** ❌ | - | - | - | **隐藏** | 缩放中旋转柄隐藏 |

**关键规律：** 缩放中只能改变大小，只显示当前操作的点，其他所有控制点隐藏。

**示例：** 如果拖拽"左上角缩放点"：
- 左上角缩放点：可见，十字光标
- 其他7个缩放点：隐藏
- 旋转柄：隐藏

---

### 2.7 旋转中（正在拖拽旋转柄）

| 当前状态 | 可交互属性 | 属性可见 | 鼠标样式 | 允许的操作 | 目标状态 | 视觉反馈 | 备注说明 |
|---------|-----------|---------|---------|-----------|---------|---------|---------|
| **旋转中** | 填充区域 | 是 | - | - | - | 随对象旋转 | 填充区域随对象旋转，但不响应交互 |
| 旋转中 | 文本内容区 | 是 | - | - | - | 随对象旋转 | 文本随对象旋转 |
| 旋转中 | 边框 | 是 | - | - | - | 随对象旋转 | 边框随对象旋转 |
| 旋转中 | 左上角缩放点 | **否** ❌ | - | - | - | **隐藏** | **旋转中所有缩放点隐藏** |
| 旋转中 | 右上角缩放点 | **否** ❌ | - | - | - | **隐藏** | 旋转中所有缩放点隐藏 |
| 旋转中 | 右下角缩放点 | **否** ❌ | - | - | - | **隐藏** | 旋转中所有缩放点隐藏 |
| 旋转中 | 左下角缩放点 | **否** ❌ | - | - | - | **隐藏** | 旋转中所有缩放点隐藏 |
| 旋转中 | 上边缩放点 | **否** ❌ | - | - | - | **隐藏** | 旋转中所有缩放点隐藏 |
| 旋转中 | 右边缩放点 | **否** ❌ | - | - | - | **隐藏** | 旋转中所有缩放点隐藏 |
| 旋转中 | 下边缩放点 | **否** ❌ | - | - | - | **隐藏** | 旋转中所有缩放点隐藏 |
| 旋转中 | 左边缩放点 | **否** ❌ | - | - | - | **隐藏** | 旋转中所有缩放点隐藏 |
| 旋转中 | 旋转柄 | **是** ✅ | 旋转光标（圆形箭头） | 拖拽中 / 释放鼠标 / 按ESC键 | 旋转中 / 选中态 / 选中态 | 实时显示角度提示（如45°） | 持续拖拽旋转；释放或ESC完成 |

**关键规律：** 旋转中只能改变角度，所有缩放点隐藏，只显示旋转柄。

---

## 三、关键修正说明

### 3.1 与之前版本的主要差异

| 项目 | 之前版本（错误） | 修正版本（正确） | 依据 |
|-----|---------------|---------------|------|
| **默认态的鼠标样式** | "默认光标" | **无**（"-"） | 默认态时鼠标不在对象上 |
| **编辑态的控制点** | "隐藏" | **"可见且可操作"** | 实际观察：编辑态时8个缩放点+旋转柄均可见 |
| **缩放中的鼠标样式** | "双向箭头（↖↘）" | **"十字光标（➕）"** | 实际观察：任何缩放点拖拽时都是十字光标 |
| **缩放中其他控制点** | "可见" | **"隐藏"** | 实际观察：只有当前操作的点可见 |
| **缩放时的对象透明度** | 未提及 | **"半透明"** | 实际观察：缩放时对象变半透明 |
| **移动中的控制点** | 未明确 | **"全部隐藏"** | 实际观察：移动中所有控制点消失 |

### 3.2 新发现的细节

#### 细节1：编辑态的双击行为差异

| 双击位置 | 行为 | 视觉效果 | 光标样式 |
|---------|------|---------|---------|
| **双击文字"123"** | 文字全选 | 文字背景变蓝 | 正常光标（不是工字型） |
| **双击空白区域** | 光标定位到文字尾部 | 无高亮 | 工字型光标 |

#### 细节2：工字型光标的热区

**观察：** 工字型光标的有效区域非常小，几乎等于文本字符的精确边界。

```
┌──────────────────────┐
│                      │
│   工字型光标热区      │  ← 这个区域很小
│   ↓                  │
│   Hello 123          │
│   ▌                  │
│                      │
│  ← 移出即变四向箭头   │
└──────────────────────┘
```

**影响：** 在编辑态时，稍微移出文本区域，光标立即变为移动光标，可以拖拽移动整个对象。

#### 细节3：变换中的排他性规律

**核心规律：** "移动中、缩放中、旋转中"这三个状态具有排他性，一次只能做一件事。

| 状态 | 唯一操作 | 控制点状态 |
|-----|---------|----------|
| **移动中** | 只能改变位置 | 所有控制点消失 |
| **缩放中** | 只能改变大小 | 只显示当前操作的点 |
| **旋转中** | 只能改变角度 | 只显示旋转柄 |

---

## 四、状态转换汇总

### 4.1 核心状态转换

| 从状态 | 到状态 | 触发条件 | 说明 |
|-------|-------|---------|------|
| 默认态 | 悬停态 | 鼠标进入对象区域 | - |
| 悬停态 | 默认态 | 鼠标离开对象区域 | - |
| 悬停态 | 选中态 | 单击对象 | - |
| 悬停态 | 编辑态 | 双击文本区 | - |
| 悬停态 | 移动中 | 拖拽对象主体 | - |
| 选中态 | 默认态 | 点击外部 / 按ESC键 | - |
| 选中态 | 编辑态 | 双击文本区 / 按F2键 | - |
| 选中态 | 移动中 | 拖拽对象主体 | - |
| 选中态 | 缩放中 | 拖拽任意缩放点 | - |
| 选中态 | 旋转中 | 拖拽旋转柄 | - |
| 编辑态 | 选中态 | 按ESC键 / 点击外部 | - |
| 编辑态 | 移动中 | 拖拽填充区（非文本区） | 鼠标移出文本热区 |
| 编辑态 | 缩放中 | 拖拽任意缩放点 | **编辑态时可直接缩放** |
| 编辑态 | 旋转中 | 拖拽旋转柄 | **编辑态时可直接旋转** |
| 移动中 | 选中态 | 释放鼠标 / 按ESC键 | - |
| 缩放中 | 选中态 | 释放鼠标 / 按ESC键 | - |
| 旋转中 | 选中态 | 释放鼠标 / 按ESC键 | - |

### 4.2 特殊转换：编辑态的灵活性

**重要发现：** 编辑态是最灵活的状态，可以直接转换到：
- 移动中（拖拽填充区）
- 缩放中（拖拽缩放点）
- 旋转中（拖拽旋转柄）

**这说明：** 编辑态并不"锁定"对象的几何变换能力。

---

## 五、属性可见性矩阵

### 5.1 控制点可见性总览

| 状态 ↓ / 控制点 → | 8个缩放点 | 旋转柄 | 说明 |
|-----------------|----------|-------|------|
| **默认态** | ❌ 不显示 | ❌ 不显示 | - |
| **悬停态** | ❌ 不显示 | ❌ 不显示 | - |
| **选中态** | ✅ 全部显示 | ✅ 显示 | 共9个控制点 |
| **编辑态** | ✅ **全部显示** | ✅ **显示** | **修正：编辑态时控制点可见** |
| **移动中** | ❌ **全部隐藏** | ❌ **隐藏** | 移动中所有控制点消失 |
| **缩放中** | ⚠️ **仅1个可见** | ❌ **隐藏** | 只显示正在操作的点 |
| **旋转中** | ❌ **全部隐藏** | ✅ **显示** | 只显示旋转柄 |

### 5.2 鼠标样式矩阵

| 状态 ↓ / 属性 → | 填充区域 | 文本内容区 | 缩放点 | 旋转柄 |
|---------------|---------|-----------|-------|-------|
| **默认态** | - | - | - | - |
| **悬停态** | 移动光标 | 工字型光标 | - | - |
| **选中态** | 移动光标 | 工字型光标 | 缩放光标 | 旋转光标 |
| **编辑态** | 移动光标 | 工字型光标 | 缩放光标 | 旋转光标 |
| **移动中** | 移动光标 | - | - | - |
| **缩放中** | - | - | **十字光标** ➕ | - |
| **旋转中** | - | - | - | 旋转光标 |

---

## 六、Excel 工作簿建议

### 6.1 Sheet 结构

```
文本框交互规则（修正版）.xlsx

├─ Sheet 1: 【修正说明】
│    ↑ 本次修正的内容和依据
│
├─ Sheet 2: 【完整规则表】
│    ↑ 所有规则（按状态分组）
│
├─ Sheet 3: 【状态转换汇总】
│    ↑ 状态转换快速查询
│
├─ Sheet 4: 【控制点可见性矩阵】
│    ↑ 7个状态 × 9个控制点
│
├─ Sheet 5: 【鼠标样式矩阵】
│    ↑ 7个状态 × 各属性
│
└─ Sheet 6: 【对比旧版本】
     ↑ 列出所有修正项
```

---

## 七、总结

### 7.1 关键修正点

✅ **修正1：默认态不应有鼠标样式**
- 原因：鼠标不在对象上

✅ **修正2：编辑态时控制点可见**
- 原因：实际观察，编辑态可以直接拖拽缩放/旋转

✅ **修正3：缩放时只显示当前操作的点**
- 原因：其他控制点隐藏，对象变半透明

✅ **修正4：缩放时鼠标是十字光标**
- 原因：不是双向箭头，而是十字

✅ **修正5：移动中所有控制点消失**
- 原因：移动时只专注位置变化

### 7.2 新发现的规律

🎯 **规律1：变换状态的排他性**
- 移动中 = 只能移动
- 缩放中 = 只能缩放
- 旋转中 = 只能旋转

🎯 **规律2：编辑态的灵活性**
- 编辑态时控制点全部可见
- 可以直接从编辑态进行移动/缩放/旋转

🎯 **规律3：工字型光标的热区极小**
- 仅限于文本字符边界
- 稍微移出就变为移动光标

---

**本修正版本基于实际 PPT 行为观察，确保了规则的准确性。**
