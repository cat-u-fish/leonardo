# PPT 对象可交互属性完整枚举（M 维度）

> 本文档是"内容容器交互规则"框架的第三步：枚举所有"可交互属性"（M 维度）
>
> **核心理念：** 用户不是与"对象"交互，而是与对象的"特定属性区域"交互

---

## 一、可交互属性的定义

### 1.1 什么是"可交互属性"？

**可交互属性（Interactive Property / Hotspot）** 是对象上的一个**可识别的区域或元素**，具有以下特征：

1. **空间可定位**：有明确的坐标范围或几何区域
2. **鼠标可感知**：鼠标进入该区域时，系统能检测到
3. **视觉可反馈**：悬停时可能改变光标样式或显示视觉提示
4. **操作可触发**：用户可以在该区域执行特定操作（点击、拖拽等）

### 1.2 属性 vs 对象

传统视角：
```
用户 → 操作"对象"（如：点击文本框）
```

精确视角：
```
用户 → 操作对象的"某个属性"（如：点击文本框的"文本内容区"）
```

**关键区别：**
- 同一个"点击"操作，作用在文本框的"文本内容区"和"填充区"，效果完全不同
- 同一个"拖拽"操作，作用在"主体"和"缩放点"，效果完全不同

### 1.3 属性的形式化表示

每个可交互属性可以用以下结构定义：

```typescript
interface InteractiveProperty {
  id: string;                      // 属性唯一标识
  name: string;                    // 属性名称
  category: PropertyCategory;      // 属性类别
  applicableObjects: ObjectType[]; // 适用的对象类型

  // 几何定义
  geometry: {
    type: 'region' | 'point' | 'line' | 'handle';
    bounds?: Rect;                 // 区域边界（如果是region）
    position?: Point;              // 位置（如果是point）
  };

  // 在不同状态下的行为
  behaviorInState: {
    [state: State]: {
      visible: boolean;            // 是否可见
      cursor: CursorStyle;         // 鼠标光标样式
      allowedEvents: Event[];      // 允许的事件
      eventHandlers: {             // 事件处理（状态转换）
        [event: Event]: State;
      };
      visualFeedback?: string;     // 视觉反馈（如高亮、提示）
    };
  };
}
```

---

## 二、通用可交互属性（适用于大部分对象）

### 2.1 主体区域（Body / Fill Area）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P01` |
| **名称** | 填充区域 / 主体区域 |
| **描述** | 对象的主要可视区域，不包括控制点和边框 |
| **适用对象** | 文本框、形状、图片、表格、图表等（几乎所有对象） |
| **几何类型** | Region（区域） |

**在不同状态下的表现：**

| 状态 | 可见性 | 光标样式 | 允许的操作 | 操作结果 |
|-----|-------|---------|----------|---------|
| **S0: Idle** | ✅ | Default | - | - |
| **S1: Hovered** | ✅ | **Move** (四向箭头) | Click → S2<br>Drag → S4 | 单击选中<br>拖拽移动 |
| **S2: Selected** | ✅ | **Move** | Drag → S4 | 拖拽移动 |
| **S3: Editing** | ✅ | Default | Click_Outside → S2 | 点击外部退出编辑 |
| **S4: Moving** | ✅ | **Move** | Dragging → S4<br>MouseUp → S2 | 持续移动<br>释放完成 |
| **S5: Resizing** | ❌ | - | - | （此时操作缩放点，主体不响应） |
| **S6: Rotating** | ❌ | - | - | （此时操作旋转柄，主体不响应） |

---

### 2.2 文本内容区（Text Content Area）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P02` |
| **名称** | 文本内容区域 |
| **描述** | 对象内的文本字符占据的区域 |
| **适用对象** | 文本框、形状（带文本）、艺术字、表格单元格 |
| **几何类型** | Region（文本边界框） |
| **前提条件** | 对象必须包含文本（`hasText == true`） |

**在不同状态下的表现：**

| 状态 | 可见性 | 光标样式 | 允许的操作 | 操作结果 |
|-----|-------|---------|----------|---------|
| **S0: Idle** | ✅ | Default | - | - |
| **S1: Hovered** | ✅ | **I-beam** (工字光标) | Click → S2<br>DoubleClick → S3 | 单击选中对象<br>双击进入编辑 |
| **S2: Selected** | ✅ | **I-beam** | DoubleClick → S3<br>F2 → S3 | 双击/F2进入编辑 |
| **S3: Editing** | ✅ | **I-beam** | TextInput → S3<br>Click → 移动光标<br>Drag → 选中文本 | 输入文字<br>点击定位<br>拖拽选中 |
| **S4: Moving** | ✅ | Move | - | （随对象移动，不响应） |
| **S5/S6** | ✅ | - | - | （随对象变换，不响应） |

**注意：**
- 在 `S3: Editing` 状态下，文本内容区成为**主要交互区域**
- 此时缩放点、旋转柄等控制点**隐藏或不可操作**

---

### 2.3 边框/轮廓（Border / Stroke）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P03` |
| **名称** | 边框 / 轮廓线 |
| **描述** | 对象的外边界线 |
| **适用对象** | 形状、文本框、图片等 |
| **几何类型** | Line（闭合路径） |
| **前提条件** | 对象有边框（`hasBorder == true`） |

**在不同状态下的表现：**

| 状态 | 可见性 | 光标样式 | 允许的操作 | 操作结果 |
|-----|-------|---------|----------|---------|
| **S1: Hovered** | ✅ | **Move** | Click → S2<br>Drag → S4 | （行为通常与主体区域相同） |
| **S2: Selected** | ✅ | Move | Drag → S4 | 拖拽移动 |
| **S3: Editing** | ✅（虚线） | - | - | 显示但不可操作 |

**注意：**
- 在大部分情况下，边框的交互行为与"主体区域"相同
- 某些高级编辑器可能允许单独编辑边框（如调整边框宽度）

---

### 2.4 缩放控制点（Resize Handles）

对象在 `S2: Selected` 状态下，会显示**8个缩放控制点**：

#### 2.4.1 四个角点（Corner Handles）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P04a`, `P04b`, `P04c`, `P04d` |
| **名称** | 左上角点、右上角点、右下角点、左下角点 |
| **描述** | 对象四个角的缩放控制点 |
| **适用对象** | 几乎所有对象 |
| **几何类型** | Point（小圆点或方块） |
| **前提条件** | 对象处于 `S2: Selected` 状态，且未被锁定 |

**在不同状态下的表现：**

| 状态 | 可见性 | 光标样式 | 允许的操作 | 操作结果 |
|-----|-------|---------|----------|---------|
| **S0: Idle** | ❌ | - | - | 不显示 |
| **S1: Hovered** | ❌ | - | - | 不显示 |
| **S2: Selected** | ✅ | **Resize** (双向斜箭头) | Drag → S5<br>Shift+Drag → S5（等比） | 拖拽开始缩放<br>按住Shift等比缩放 |
| **S3: Editing** | ❌ 或 ⚠️（淡化） | - | - | 隐藏或不可操作 |
| **S5: Resizing** | ✅ | **Resize** | Dragging → S5<br>MouseUp → S2 | 持续缩放<br>释放完成 |

**光标样式细节：**
- 左上角点：↖↘ (nwse-resize)
- 右上角点：↗↙ (nesw-resize)
- 右下角点：↖↘ (nwse-resize)
- 左下角点：↗↙ (nesw-resize)

---

#### 2.4.2 四个边点（Edge Handles）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P05a`, `P05b`, `P05c`, `P05d` |
| **名称** | 上边点、右边点、下边点、左边点 |
| **描述** | 对象四条边中点的缩放控制点 |
| **几何类型** | Point |

**在 S2: Selected 状态下的表现：**

| 边点位置 | 光标样式 | 操作效果 |
|---------|---------|---------|
| **上边点** | ↕ (ns-resize) | 向上/下拉伸高度 |
| **右边点** | ↔ (ew-resize) | 向右/左拉伸宽度 |
| **下边点** | ↕ (ns-resize) | 向下/上拉伸高度 |
| **左边点** | ↔ (ew-resize) | 向左/右拉伸宽度 |

**注意：**
- 边点通常用于**单向拉伸**（不保持宽高比）
- 角点通常用于**等比缩放**（按住Shift）或**自由缩放**

---

### 2.5 旋转控制手柄（Rotation Handle）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P06` |
| **名称** | 旋转手柄 |
| **描述** | 对象顶部中心的旋转控制点（通常是一个圆形图标） |
| **适用对象** | 几乎所有对象 |
| **几何类型** | Point |
| **前提条件** | 对象处于 `S2: Selected` 状态，且未被锁定 |

**在不同状态下的表现：**

| 状态 | 可见性 | 光标样式 | 允许的操作 | 操作结果 |
|-----|-------|---------|----------|---------|
| **S2: Selected** | ✅ | **Rotate** (圆形箭头 🔄) | Drag → S6<br>Shift+Drag → S6（15°增量） | 拖拽开始旋转<br>按住Shift以15°为单位旋转 |
| **S6: Rotating** | ✅ | **Rotate** | Dragging → S6<br>MouseUp → S2 | 持续旋转<br>释放完成 |
| **其他状态** | ❌ | - | - | 隐藏 |

---

### 2.6 调整手柄（Adjustment Handles / Yellow Diamonds）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P07` |
| **名称** | 调整手柄（黄色菱形） |
| **描述** | 某些形状特有的几何调整点，用于改变形状的特定参数 |
| **适用对象** | 部分形状（如梯形、圆角矩形、箭头、标注等） |
| **几何类型** | Point（黄色菱形标记） |
| **示例** | - 圆角矩形：调整圆角半径<br>- 梯形：调整倾斜角度<br>- 箭头：调整箭头头部大小 |

**在 S2: Selected 状态下的表现：**

| 光标样式 | 操作效果 |
|---------|---------|
| **Move** 或特殊光标 | 拖拽调整形状参数，但**不改变尺寸**，**不触发状态转换**（保持S2） |

**注意：**
- 调整手柄的操作**不触发状态转换**
- 操作过程中始终处于 `S2: Selected` 状态
- 这与缩放点（触发S5）、旋转柄（触发S6）不同

---

## 三、特殊对象的专属可交互属性

### 3.1 连接线（Connector）专属属性

#### 3.1.1 连接点（Connection Points / Endpoints）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P10a`, `P10b` |
| **名称** | 起点、终点 |
| **描述** | 连接线的两个端点，可附着到其他对象 |
| **几何类型** | Point |

**在 S2: Selected 状态下：**

| 光标样式 | 操作效果 |
|---------|---------|
| **Crosshair** (十字光标) | 拖拽端点可重新连接到其他对象的连接点 |

#### 3.1.2 路径节点（Path Vertices）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P11` |
| **名称** | 路径中间节点 |
| **描述** | 肘形连接线的转折点 |
| **几何类型** | Point |

**在 S2: Selected 状态下：**

| 光标样式 | 操作效果 |
|---------|---------|
| **Move** | 拖拽可调整连接线的路径 |

---

### 3.2 自由曲线（Curve / Freeform）专属属性

#### 3.2.1 路径编辑节点（Path Editing Points）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P12` |
| **名称** | 路径节点 |
| **描述** | 自由曲线的控制点，用于编辑路径形状 |
| **前提条件** | 进入"路径编辑模式"（S9: PathEditing） |

**在 S9: PathEditing 状态下：**

| 光标样式 | 操作效果 |
|---------|---------|
| **Move** | 拖拽节点调整曲线形状 |

#### 3.2.2 贝塞尔控制柄（Bezier Control Handles）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P13` |
| **名称** | 贝塞尔曲线控制柄 |
| **描述** | 从路径节点延伸出的控制柄，调整曲线的曲率 |
| **几何类型** | Point（小圆点，通过线段连接到节点） |

---

### 3.3 表格（Table）专属属性

#### 3.3.1 单元格（Cell）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P20` |
| **名称** | 表格单元格 |
| **描述** | 表格的一个单元格区域 |
| **几何类型** | Region |

**在不同状态下的表现：**

| 状态 | 光标样式 | 允许的操作 | 操作结果 |
|-----|---------|----------|---------|
| **S1: Hovered** | **I-beam** | Click → 选中单元格<br>DoubleClick → S10 (CellEditing) | 选中单元格<br>进入单元格编辑 |
| **S10: CellEditing** | **I-beam** | TextInput<br>Tab → 下一个单元格<br>Enter → 下一行 | 输入文本<br>切换单元格 |

#### 3.3.2 行边界线（Row Border）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P21` |
| **名称** | 行边界线 |
| **描述** | 表格行与行之间的分隔线 |
| **几何类型** | Line |

**在 S2: Selected 状态下：**

| 光标样式 | 操作效果 |
|---------|---------|
| **↕ (ns-resize)** | 拖拽可调整行高 |

#### 3.3.3 列边界线（Column Border）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P22` |
| **名称** | 列边界线 |
| **描述** | 表格列与列之间的分隔线 |
| **几何类型** | Line |

**在 S2: Selected 状态下：**

| 光标样式 | 操作效果 |
|---------|---------|
| **↔ (ew-resize)** | 拖拽可调整列宽 |

---

### 3.4 图表（Chart）专属属性

#### 3.4.1 图表整体区域

| 属性 | 值 |
|-----|---|
| **属性ID** | `P30` |
| **名称** | 图表区域 |
| **描述** | 整个图表对象 |
| **光标样式** | Move |
| **操作** | 单击选中，拖拽移动，双击打开数据编辑 |

#### 3.4.2 图表元素（Chart Elements）

| 属性ID | 名称 | 描述 | 操作 |
|-------|------|------|------|
| `P31` | 数据系列 | 柱形、折线、饼图扇区等 | 单击选中该系列 |
| `P32` | 图例 | 图例区域 | 单击选中，可拖拽移动 |
| `P33` | 坐标轴 | X轴、Y轴 | 单击选中，双击编辑格式 |
| `P34` | 数据标签 | 数据点的标签 | 单击选中，可拖拽移动 |

---

### 3.5 SmartArt 专属属性

#### 3.5.1 SmartArt 节点

| 属性 | 值 |
|-----|---|
| **属性ID** | `P40` |
| **名称** | SmartArt 节点/形状 |
| **描述** | SmartArt 图形中的单个节点 |
| **光标样式** | Move |
| **操作** | 单击选中节点，双击编辑文本 |

#### 3.5.2 SmartArt 连接线

| 属性 | 值 |
|-----|---|
| **属性ID** | `P41` |
| **名称** | SmartArt 连接线 |
| **描述** | 节点之间的连接线 |
| **光标样式** | Default |
| **操作** | 通常不可单独编辑 |

---

### 3.6 媒体对象（Video/Audio）专属属性

#### 3.6.1 播放控制条（Playback Controls）

| 属性 | 值 |
|-----|---|
| **属性ID** | `P50` |
| **名称** | 播放控制条 |
| **描述** | 视频/音频的播放、暂停、进度条等控制 |
| **光标样式** | Pointer (手型) |
| **操作** | 点击播放/暂停，拖拽进度条 |
| **可见条件** | 仅在放映模式或预览时显示 |

---

## 四、可交互属性的分类体系

### 4.1 按功能分类

| 类别 | 属性列表 | 作用 |
|-----|---------|------|
| **主体交互** | 填充区域、边框 | 选中、移动对象 |
| **几何变换** | 缩放点（8个）、旋转柄 | 调整对象尺寸、角度 |
| **形状调整** | 调整手柄（黄色菱形） | 改变形状参数 |
| **内容编辑** | 文本内容区、单元格 | 编辑文本内容 |
| **路径编辑** | 路径节点、贝塞尔控制柄 | 编辑曲线形状 |
| **结构编辑** | 连接点、行列边界线 | 调整结构关系 |
| **嵌套选择** | SmartArt节点、图表元素 | 选择子对象 |
| **媒体控制** | 播放控制条 | 控制播放 |

### 4.2 按几何类型分类

| 几何类型 | 示例属性 |
|---------|---------|
| **Region（区域）** | 填充区域、文本内容区、单元格 |
| **Point（点）** | 缩放点、旋转柄、路径节点、调整手柄 |
| **Line（线）** | 边框、行列边界线、连接线 |
| **Handle（手柄）** | 旋转柄、贝塞尔控制柄 |

### 4.3 按优先级（Z-order）分类

当多个属性区域重叠时，优先级决定响应哪个属性：

| 优先级 | 属性类型 | 示例 |
|-------|---------|------|
| **1（最高）** | 控制点 | 缩放点、旋转柄 |
| **2** | 特殊交互区 | 文本内容区、调整手柄 |
| **3** | 边界 | 边框、行列边界线 |
| **4（最低）** | 主体 | 填充区域 |

**规则：** 当鼠标位置同时满足多个属性的几何范围时，响应优先级最高的属性。

**示例：**
- 鼠标悬停在"缩放点"附近（5px范围内），即使同时也在"填充区域"内，也优先响应"缩放点"

---

## 五、M × N 矩阵的单元格结构

现在我们有了：
- **M 维度**：可交互属性（P01, P02, ..., P50）
- **N 维度**：状态（S0, S1, S2, ..., S6）

**矩阵单元格 `Matrix[M, N]` 应包含：**

```typescript
interface MatrixCell {
  // 1. 可见性
  visible: boolean;                // 该属性在此状态下是否可见/可用

  // 2. 光标样式
  cursorStyle: CursorStyle;        // 鼠标悬停时的光标样式
  // 可能值: 'default', 'move', 'pointer', 'text' (I-beam),
  //        'nwse-resize', 'nesw-resize', 'ns-resize', 'ew-resize',
  //        'grab', 'grabbing', 'crosshair', 'not-allowed', etc.

  // 3. 允许的事件
  allowedEvents: Event[];          // 该属性在此状态下可响应的事件

  // 4. 事件 → 状态转换映射
  eventTransitions: {
    [event: Event]: State;         // 事件触发的状态转换
  };

  // 5. 视觉反馈（可选）
  visualFeedback?: {
    hover?: string;                // 悬停时的视觉效果（如高亮颜色）
    active?: string;               // 操作中的视觉效果
  };

  // 6. 操作约束（可选）
  constraints?: {
    requiresModifier?: 'Shift' | 'Ctrl' | 'Alt';  // 需要按住修饰键
    conditionalOn?: string;        // 依赖的条件（如 hasText == true）
  };
}
```

---

## 六、示例：文本框对象的完整 M × N 矩阵

**对象：文本框（包含文字"Hello"）**

### 矩阵表格

| 属性 ↓ / 状态 → | S0: Idle | S1: Hovered | S2: Selected | S3: Editing |
|---------------|---------|------------|-------------|------------|
| **P01: 填充区域** | ❌ 不响应 | ✅ Move光标<br>Click→S2<br>Drag→S4 | ✅ Move光标<br>Drag→S4 | ✅ 可见<br>不响应 |
| **P02: 文本内容区** | ❌ 不响应 | ✅ I-beam光标<br>Click→S2<br>DblClick→S3 | ✅ I-beam光标<br>DblClick→S3<br>F2→S3 | ✅ I-beam光标<br>TextInput<br>Click移动光标<br>Drag选中文本 |
| **P04: 缩放点（角）** | ❌ 不显示 | ❌ 不显示 | ✅ Resize光标<br>Drag→S5 | ❌ 隐藏 |
| **P05: 缩放点（边）** | ❌ 不显示 | ❌ 不显示 | ✅ Resize光标<br>Drag→S5 | ❌ 隐藏 |
| **P06: 旋转柄** | ❌ 不显示 | ❌ 不显示 | ✅ Rotate光标<br>Drag→S6 | ❌ 隐藏 |

### 详细单元格示例

**`Matrix[P02: 文本内容区, S2: Selected]`:**

```json
{
  "visible": true,
  "cursorStyle": "text",  // I-beam
  "allowedEvents": ["DoubleClick", "Press_F2"],
  "eventTransitions": {
    "DoubleClick": "S3",
    "Press_F2": "S3"
  },
  "visualFeedback": {
    "hover": "文本内容区高亮"
  }
}
```

**`Matrix[P04a: 左上角缩放点, S2: Selected]`:**

```json
{
  "visible": true,
  "cursorStyle": "nwse-resize",  // ↖↘
  "allowedEvents": ["Drag"],
  "eventTransitions": {
    "Drag": "S5"
  },
  "constraints": {
    "requiresModifier": "Shift"  // 按住Shift时等比缩放
  }
}
```

**`Matrix[P01: 填充区域, S3: Editing]`:**

```json
{
  "visible": true,
  "cursorStyle": "default",
  "allowedEvents": [],  // 不响应任何操作（此时焦点在文本编辑）
  "eventTransitions": {}
}
```

---

## 七、属性冲突与优先级解决

### 7.1 空间重叠问题

**场景：** 鼠标同时位于"文本内容区"和"填充区域"内（文本周围有空白）

**解决方案：**

#### 方案A：精确检测（推荐）
```
if (鼠标位置 在文本字符边界内):
    响应 P02: 文本内容区 → I-beam光标
else:
    响应 P01: 填充区域 → Move光标
```

#### 方案B：优先级规则
```
优先级：P02 (文本内容区) > P01 (填充区域)
当两者都命中时，优先响应文本内容区
```

### 7.2 控制点优先级

**规则：** 控制点（缩放点、旋转柄）的命中区域通常有一个"热区范围"（如5-8px半径）

```typescript
const HOTSPOT_RADIUS = 6; // 像素

function detectProperty(mousePos: Point, object: Object, state: State): Property {
  if (state === State.Selected) {
    // 优先检测控制点
    for (let handle of [resizeHandles, rotateHandle]) {
      if (distance(mousePos, handle.position) <= HOTSPOT_RADIUS) {
        return handle;
      }
    }
  }

  // 然后检测特殊交互区（如文本内容区）
  if (isInsideTextContent(mousePos)) {
    return textContentProperty;
  }

  // 最后检测主体区域
  if (isInsideBody(mousePos)) {
    return bodyProperty;
  }

  return null;
}
```

---

## 八、总结与下一步

### ✅ 已完成：可交互属性（M 维度）枚举

1. **通用属性**（6类）：填充区、文本内容、边框、缩放点×8、旋转柄、调整手柄
2. **连接线属性**：连接点、路径节点
3. **曲线属性**：路径编辑节点、贝塞尔控制柄
4. **表格属性**：单元格、行边界、列边界
5. **图表属性**：图表区域、数据系列、图例等
6. **SmartArt属性**：节点、连接线
7. **媒体属性**：播放控制条

### ✅ 定义了矩阵单元格结构

```
Matrix[M, N] = {
  visible,
  cursorStyle,
  allowedEvents,
  eventTransitions,
  visualFeedback,
  constraints
}
```

### 🎯 下一步：构建完整的 M × N 矩阵

现在我们有了所有要素：
- **M 维度**：可交互属性（~20个核心属性）
- **N 维度**：状态（7个核心状态）
- **单元格结构**：6个字段

**可以开始构建：**
1. **选择一个典型对象**（如：文本框、形状、表格）
2. **填充完整的 M × N 矩阵**
3. **提供可视化表格和代码实现**

你想从哪个对象开始构建完整矩阵？
