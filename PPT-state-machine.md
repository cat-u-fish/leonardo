# PPT 对象状态机完整定义

> 本文档是"内容容器交互规则"框架的第二步：定义完备且统一的状态机（N维度）

---

## 一、状态分类体系

状态可以分为两大类：
- **核心交互状态** (Core Interaction States)：瞬时的、互斥的、由用户交互触发
- **修饰状态** (Modifier States)：持久的、可组合的、作为对象属性存在

---

## 二、核心交互状态 (Core Interaction States)

**特征：**
- 同一时刻，一个对象只能处于**一种**核心交互状态
- 这些状态是**互斥的**
- 由用户的鼠标/键盘操作触发
- 状态是**瞬时的**，会随交互变化

### 2.1 默认态 (Default / Idle)
**定义：** 对象未被选中、未被悬停、无任何交互的初始状态

**视觉特征：**
- 无选中框、无控制点
- 显示对象的常规外观
- 鼠标未与对象发生任何交互

**进入条件：**
- 对象刚创建完成
- 用户点击画布空白区域（取消选中）
- 用户选中其他对象
- 按 ESC 键退出其他状态

**可转换到：**
- 悬停态（鼠标移入）
- 选中态（单击对象）
- 编辑态（双击可编辑对象）

---

### 2.2 悬停态 (Hovered)
**定义：** 鼠标指针悬停在对象上方，但未点击

**视觉特征：**
- 对象可能显示轻微的视觉反馈（取决于设计）
- 鼠标光标根据悬停的**属性区域**变化（关键！）
- 可能显示悬停提示（如超链接提示）

**进入条件：**
- 鼠标从对象外移入对象区域
- 对象处于默认态

**可转换到：**
- 默认态（鼠标移出）
- 选中态（单击）
- 编辑态（双击）
- 移动中（拖拽填充区域）
- 缩放中（拖拽缩放点）
- 旋转中（拖拽旋转手柄）

**注意：**
- 悬停态是"临时"状态，鼠标移出即消失
- **不同属性区域**的悬停会有不同的光标反馈（M×N矩阵的关键）

---

### 2.3 选中态 (Selected)
**定义：** 对象被选中，显示选择框和控制点

**视觉特征：**
- 显示**边界框**（Bounding Box）
- 显示**8个缩放控制点**（Corner & Edge Handles）
- 显示**旋转手柄**（Rotation Handle）
- 可能显示对齐辅助线
- 选中对象的属性面板激活

**进入条件：**
- 单击对象（从默认态或悬停态）
- Tab 键切换对象
- Ctrl+A 全选
- 框选操作

**可转换到：**
- 默认态（点击空白区域、ESC键、选中其他对象）
- 编辑态（双击、F2键）
- 移动中（拖拽对象主体）
- 缩放中（拖拽缩放点）
- 旋转中（拖拽旋转手柄）
- 悬停态（鼠标移出后再移入）

**子状态：**
- **单选态** (Single Selection)：一个对象被选中
- **多选态** (Multiple Selection)：多个对象被选中
  - 多选时，控制点行为可能不同
  - 某些操作（如编辑文本）不可用

**注意：**
- 选中态是"持久"状态，直到主动取消
- 选中态下，对象的不同**属性区域**悬停仍有不同光标

---

### 2.4 变换中 (Transforming)
**定义：** 对象正在进行几何变换的过程中

**这是一个"父状态"，包含三个子状态：**

#### 2.4.1 移动中 (Moving / Dragging)
**定义：** 对象正在被拖拽移动

**视觉特征：**
- 对象跟随鼠标移动
- 可能显示对齐辅助线（Smart Guides）
- 可能显示位置坐标提示
- 鼠标光标：移动光标（四向箭头）

**进入条件：**
- 在选中态，拖拽对象主体
- 在悬停态，拖拽对象主体
- 使用方向键微调

**退出条件：**
- 释放鼠标（返回选中态）
- 按 ESC 取消移动（返回原位置）

**约束：**
- 按住 Shift：锁定水平/垂直移动
- 按住 Ctrl：复制而非移动（Windows）/ Alt（Mac）

---

#### 2.4.2 缩放中 (Resizing / Scaling)
**定义：** 对象正在被拉伸或缩放

**视觉特征：**
- 对象尺寸实时变化
- 可能显示尺寸提示（宽×高）
- 鼠标光标：双向箭头（方向取决于控制点）

**进入条件：**
- 在选中态，拖拽8个缩放控制点之一

**退出条件：**
- 释放鼠标（返回选中态）
- 按 ESC 取消缩放

**约束：**
- 按住 Shift：等比例缩放
- 按住 Ctrl：从中心点缩放
- 拖拽角点 vs 边点：行为不同

**子类型：**
- **等比缩放** (Proportional Scaling)
- **自由拉伸** (Free Resizing)
- **单向拉伸** (One-dimensional Resizing)

---

#### 2.4.3 旋转中 (Rotating)
**定义：** 对象正在被旋转

**视觉特征：**
- 对象围绕旋转中心点旋转
- 可能显示旋转角度提示（如：45°）
- 鼠标光标：旋转光标（圆形箭头）

**进入条件：**
- 在选中态，拖拽旋转手柄

**退出条件：**
- 释放鼠标（返回选中态）
- 按 ESC 取消旋转

**约束：**
- 按住 Shift：以15°为单位旋转
- 旋转中心点可调整

---

### 2.5 编辑态 (Editing / Edit Mode)
**定义：** 对象的内容正在被编辑（主要针对文本或路径）

**适用对象：**
- 文本框：编辑文字内容
- 形状（带文本）：编辑形状内文字
- 表格：编辑单元格内容
- 艺术字：编辑文字
- 曲线/自由形状：编辑路径节点

**视觉特征：**
- **文本编辑**：
  - 显示文本光标（闪烁的竖线）
  - 文本可选中、可输入
  - 缩放控制点**隐藏**或**淡化**
  - 边界框可能变为虚线
- **路径编辑**：
  - 显示路径节点（Path Points）
  - 显示贝塞尔曲线控制柄

**进入条件：**
- 双击对象
- 在选中态按 F2 键
- 在悬停态单击"文本内容"属性区域

**退出条件：**
- 点击对象外部（返回选中态或默认态）
- 按 ESC 键（返回选中态）
- 选中其他对象

**约束：**
- 编辑态下，**对象的几何变换控制点被禁用**
- 但某些快捷键仍可用（如复制、粘贴）

**子类型：**
- **文本编辑态** (Text Editing)
- **路径编辑态** (Path Editing)
- **表格单元格编辑态** (Cell Editing)

---

### 2.6 预选态 (Pre-selection / Hover-to-Select)
**定义：** 在复杂对象（如表格、组合）中，鼠标悬停在子对象上，提示可点击选中该子对象

**视觉特征：**
- 子对象显示高亮边框
- 鼠标光标变化

**适用场景：**
- 表格：悬停在某一行/列/单元格
- 组合对象：悬停在组内某个元素
- SmartArt：悬停在某个节点

**进入条件：**
- 鼠标悬停在复杂对象的子元素上

**可转换到：**
- 选中子对象（单击）
- 默认态（鼠标移出）

---

### 2.7 拖入态 (Drag-Over / Drop Target)
**定义：** 外部内容正被拖拽到对象上方，对象作为潜在的"放置目标"

**视觉特征：**
- 对象显示"可接受拖放"的视觉提示
- 可能显示高亮边框或阴影

**适用场景：**
- 拖拽图片到图片占位符
- 拖拽文件到幻灯片
- 拖拽文本到文本框

**进入条件：**
- 外部内容被拖拽到对象上方

**退出条件：**
- 释放鼠标（执行放置操作）
- 拖拽离开对象区域

---

### 2.8 动画播放中 (Animating)
**定义：** 对象的动画效果正在播放（仅在幻灯片放映模式）

**视觉特征：**
- 对象按动画效果运动/变化

**进入条件：**
- 幻灯片放映时，触发对象动画

**退出条件：**
- 动画播放完成
- 退出放映模式

**注意：**
- 此状态仅在"放映模式"下存在
- 在"编辑模式"下不适用

---

## 三、修饰状态 (Modifier States / Persistent Attributes)

**特征：**
- 这些状态是**持久化的对象属性**
- 可以**组合存在**（一个对象可以同时具有多个修饰状态）
- 修饰状态会**影响**核心交互状态的行为
- 由用户主动设置，直到取消

---

### 3.1 锁定态 (Locked)
**定义：** 对象被锁定，禁止编辑和移动

**影响：**
- **无法进入**：移动中、缩放中、旋转中、编辑态
- **可以进入**：默认态、悬停态、选中态（但控制点不可操作）
- 右键菜单中"解锁"选项可用

**视觉提示：**
- 选中时，控制点显示锁定图标
- 或边界框显示锁定标识

**设置方式：**
- 右键菜单 → 锁定
- 快捷键（如 Ctrl+K）

---

### 3.2 隐藏态 (Hidden / Invisible)
**定义：** 对象在幻灯片放映时不可见（但在编辑模式仍可见）

**影响：**
- 编辑模式：对象正常显示（可能有特殊标识）
- 放映模式：对象不显示

**视觉提示：**
- 编辑模式下，对象可能显示半透明或特殊图标

**设置方式：**
- 动画窗格 → 设置为隐藏
- 对象属性 → 隐藏

---

### 3.3 成组态 (In-Group / Grouped)
**定义：** 对象属于某个组合对象的成员

**影响：**
- **单击组**：选中整个组（组级别的选中态）
- **双击组内对象**：进入"组内编辑模式"，可选中和编辑单个成员
- **组级别操作**：移动、缩放、旋转作用于整个组

**子状态：**
- **组级别选中** (Group-level Selection)：整个组被选中
- **组内对象选中** (In-group Object Selection)：进入组内，单个成员被选中

**视觉提示：**
- 组选中时：边界框包围整个组
- 组内选中时：边界框仅包围单个成员

**设置方式：**
- 选中多个对象 → Ctrl+G 组合
- 解组：Ctrl+Shift+G

---

### 3.4 母版态 (Master-Object / Template-Bound)
**定义：** 对象来自母版或版式，受母版控制

**影响：**
- 对象可能**无法删除**
- 对象可能**无法完全编辑**（某些属性受母版约束）
- "重置幻灯片"操作会恢复母版设置

**类型：**
- **母版占位符** (Master Placeholder)：完全受母版控制
- **版式占位符** (Layout Placeholder)：部分受版式控制

**视觉提示：**
- 占位符显示虚线边框
- 占位符内显示提示文字（如"单击此处添加标题"）

---

### 3.5 禁用态 (Disabled)
**定义：** 对象暂时不可交互（罕见，但在某些场景存在）

**适用场景：**
- 幻灯片放映时，非交互对象
- 动画播放期间，其他对象
- 受权限保护的对象（协作编辑时）

**影响：**
- 无法进入任何交互状态
- 鼠标事件穿透到背后对象

---

### 3.6 受保护态 (Protected)
**定义：** 对象受演示文稿保护设置约束

**影响：**
- 根据保护级别，限制编辑、移动、删除等操作
- 类似锁定态，但由文档级权限控制

---

### 3.7 动画绑定态 (Animation-Bound)
**定义：** 对象绑定了动画效果

**影响：**
- 对象在"动画窗格"中显示
- 删除对象时，关联动画也被删除
- 影响幻灯片放映时的显示时机

**视觉提示：**
- 编辑模式下，对象可能显示动画序号标识

---

## 四、状态转换规则 (State Transition Rules)

### 4.1 核心交互状态转换图

```
                  鼠标移入
默认态 (Default) ←--------→ 悬停态 (Hovered)
    ↓                           ↓
    |                       单击 |
    |                           ↓
    └----------------------→ 选中态 (Selected)
                                ↓
                    ┌───────────┼───────────┐
                双击/F2         |           |
                    ↓           ↓           ↓
                编辑态      变换中      （其他操作）
              (Editing)   (Transforming)
                    ↑           ↓
                    |      ┌────┼────┐
                    |      ↓    ↓    ↓
                    |    移动  缩放  旋转
                    |   (Move)(Resize)(Rotate)
                    |      ↓    ↓    ↓
                    └──────┴────┴────┘
                        ESC / 点击外部
                            ↓
                        选中态 / 默认态
```

### 4.2 状态互斥规则

**同一时刻，对象只能处于以下核心状态之一：**
1. 默认态
2. 悬停态
3. 选中态
4. 移动中
5. 缩放中
6. 旋转中
7. 编辑态
8. 动画播放中

**修饰状态可组合：**
- 一个对象可以同时是：**锁定态 + 成组态 + 动画绑定态**

### 4.3 修饰状态对核心状态的约束

| 修饰状态 | 禁止的核心状态 | 允许的核心状态 |
|---------|------------|------------|
| 锁定态 | 移动中、缩放中、旋转中、编辑态 | 默认态、悬停态、选中态 |
| 隐藏态 | 动画播放中（放映时） | 所有状态（编辑模式） |
| 成组态 | （组内成员）个别选中 | 组级别选中 |
| 母版态 | 删除、部分编辑 | 大部分交互 |
| 禁用态 | 所有交互状态 | 仅默认态 |

---

## 五、特殊对象的状态扩展

某些对象类型可能有**特殊的状态**：

### 5.1 表格特有状态
- **单元格编辑态** (Cell Editing)
- **行/列选中态** (Row/Column Selected)
- **调整行高/列宽中** (Resizing Row/Column)

### 5.2 视频/音频特有状态
- **播放中** (Playing)
- **暂停态** (Paused)
- **缓冲中** (Buffering)

### 5.3 超链接对象状态
- **链接悬停态** (Link Hovered)：鼠标悬停在超链接上
  - 显示链接提示
  - 光标变为手型

### 5.4 SmartArt 特有状态
- **SmartArt编辑态** (SmartArt Editing)
- **节点选中态** (Node Selected)
- **文本窗格编辑态** (Text Pane Editing)

---

## 六、状态的形式化定义

每个状态可以用以下属性定义：

```typescript
interface State {
  id: string;                    // 状态标识
  name: string;                  // 状态名称
  type: 'core' | 'modifier';     // 状态类型
  category?: string;             // 类别（如：transforming）

  // 进入条件
  entryConditions: Condition[];

  // 退出条件
  exitConditions: Condition[];

  // 视觉特征
  visualFeatures: {
    boundingBox?: boolean;       // 是否显示边界框
    controlPoints?: boolean;     // 是否显示控制点
    cursor?: CursorType;         // 光标类型
    highlight?: boolean;         // 是否高亮
  };

  // 可执行操作
  allowedActions: Action[];

  // 禁止的操作
  forbiddenActions: Action[];

  // 可转换到的状态
  transitions: string[];

  // 受修饰状态的影响
  affectedByModifiers?: {
    [modifierState: string]: StateConstraint;
  };
}
```

---

## 七、总结与下一步

### 已完成的状态枚举：

**核心交互状态：**
1. 默认态 (Default)
2. 悬停态 (Hovered)
3. 选中态 (Selected)
   - 单选态 (Single Selection)
   - 多选态 (Multiple Selection)
4. 变换中 (Transforming)
   - 移动中 (Moving)
   - 缩放中 (Resizing)
   - 旋转中 (Rotating)
5. 编辑态 (Editing)
   - 文本编辑态
   - 路径编辑态
   - 单元格编辑态
6. 预选态 (Pre-selection)
7. 拖入态 (Drag-Over)
8. 动画播放中 (Animating)

**修饰状态：**
1. 锁定态 (Locked)
2. 隐藏态 (Hidden)
3. 成组态 (In-Group)
4. 母版态 (Master-Object)
5. 禁用态 (Disabled)
6. 受保护态 (Protected)
7. 动画绑定态 (Animation-Bound)

### 下一步建议：
1. ✅ **确认状态枚举是否完整**
2. **枚举"可交互属性"（M维度）** - 这是框架的核心
3. **构建 M × N 交互矩阵** - 最终的"交互宪法"
4. **定义属性的优先级和冲突解决规则**
