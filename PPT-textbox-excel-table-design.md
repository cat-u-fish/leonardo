# 文本框交互规则 Excel 表格设计

> 本文档提供了一个可直接在 Excel 中使用的表格结构，用于呈现文本框的完整交互规则

---

## 一、表格结构设计

### 1.1 优化后的列结构

基于你的想法，我优化了表格结构，增加了"属性可见性"和"视觉反馈"列：

| 列名 | 说明 | 示例值 |
|-----|------|-------|
| **A: 状态(源)** | 当前对象所处的状态 | S0-Idle, S1-Hovered, S2-Selected |
| **B: 可交互属性(M)** | 用户鼠标悬停/操作的区域 | P01-填充区, P02-文本内容区 |
| **C: 属性可见** | 该属性在此状态下是否显示 | 是 / 否 |
| **D: 鼠标样式** | 鼠标悬停时的光标icon | Move, I-beam, Resize |
| **E: 允许的操作** | 用户可执行的键鼠操作 | Click, Drag, DoubleClick |
| **F: 状态(目标)** | 执行操作后转换到的状态 | S2-Selected, S3-Editing |
| **G: 视觉反馈** | 界面上的视觉变化 | 显示控制点、高亮边框 |
| **H: 备注** | 补充说明和约束条件 | 按住Shift等比缩放 |

---

## 二、完整的交互规则表格

### 2.1 表头说明

```
列A       列B              列C        列D          列E          列F          列G            列H
状态(源)  可交互属性(M)    属性可见   鼠标样式     允许的操作   状态(目标)   视觉反馈       备注
```

---

### 2.2 完整数据表格

以下是可以直接复制到 Excel 的完整数据：

---

#### **Sheet 1: 文本框交互规则总表**

| 状态(源) | 可交互属性(M) | 属性可见 | 鼠标样式 | 允许的操作 | 状态(目标) | 视觉反馈 | 备注 |
|---------|-------------|---------|---------|-----------|-----------|---------|------|
| **S0-Idle** | P01-填充区域 | 是 | Default | 无 | - | 无 | 对象在默认态不响应任何操作 |
| S0-Idle | P02-文本内容区 | 是 | Default | 无 | - | 无 | 对象在默认态不响应任何操作 |
| S0-Idle | P03-边框 | 是 | Default | 无 | - | 无 | 对象在默认态不响应任何操作 |
| S0-Idle | P04-缩放点(角) | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| S0-Idle | P05-缩放点(边) | 否 | - | - | - | 不显示 | 缩放点仅在选中态显示 |
| S0-Idle | P06-旋转柄 | 否 | - | - | - | 不显示 | 旋转柄仅在选中态显示 |
| | | | | | | | |
| **S1-Hovered** | P01-填充区域 | 是 | Move (四向箭头) | Click / Drag | S2 / S4 | 可选：轻微高亮边框 | 单击选中，拖拽移动 |
| S1-Hovered | P02-文本内容区 | 是 | I-beam (工字型) | Click / DoubleClick | S2 / S3 | 可选：文本区域高亮 | 单击选中对象，双击进入编辑 |
| S1-Hovered | P03-边框 | 是 | Move | Click / Drag | S2 / S4 | 同填充区域 | 行为与填充区域相同 |
| S1-Hovered | P04-缩放点(角) | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| S1-Hovered | P05-缩放点(边) | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| S1-Hovered | P06-旋转柄 | 否 | - | - | - | 不显示 | 仅在选中态显示 |
| | | | | | | | |
| **S2-Selected** | P01-填充区域 | 是 | Move | Drag / ESC / Click_Outside | S4 / S0 / S0 | 显示选择框和控制点 | 拖拽移动；ESC或点击外部取消选中 |
| S2-Selected | P02-文本内容区 | 是 | I-beam | DoubleClick / F2 | S3 / S3 | 文本区域可能高亮 | 双击或按F2进入编辑态 |
| S2-Selected | P03-边框 | 是 | Move | Drag | S4 | 同填充区域 | 行为与填充区域相同 |
| S2-Selected | P04_TL-左上角点 | 是 | Resize (↖↘) | Drag | S5 | 显示为蓝色圆点或方块 | 拖拽开始缩放；按Shift等比缩放 |
| S2-Selected | P04_TR-右上角点 | 是 | Resize (↗↙) | Drag | S5 | 显示为蓝色圆点或方块 | 拖拽开始缩放 |
| S2-Selected | P04_BR-右下角点 | 是 | Resize (↖↘) | Drag | S5 | 显示为蓝色圆点或方块 | 拖拽开始缩放 |
| S2-Selected | P04_BL-左下角点 | 是 | Resize (↗↙) | Drag | S5 | 显示为蓝色圆点或方块 | 拖拽开始缩放 |
| S2-Selected | P05_T-上边点 | 是 | Resize (↕) | Drag | S5 | 显示为蓝色圆点或方块 | 单向拉伸高度 |
| S2-Selected | P05_R-右边点 | 是 | Resize (↔) | Drag | S5 | 显示为蓝色圆点或方块 | 单向拉伸宽度 |
| S2-Selected | P05_B-下边点 | 是 | Resize (↕) | Drag | S5 | 显示为蓝色圆点或方块 | 单向拉伸高度 |
| S2-Selected | P05_L-左边点 | 是 | Resize (↔) | Drag | S5 | 显示为蓝色圆点或方块 | 单向拉伸宽度 |
| S2-Selected | P06-旋转柄 | 是 | Rotate (🔄) | Drag | S6 | 圆形图标，连接线到对象 | 按Shift以15°为单位旋转 |
| | | | | | | | |
| **S3-Editing** | P01-填充区域 | 是 | Default | 无 | - | 显示但不响应 | 编辑态时焦点在文本内容区 |
| S3-Editing | P02-文本内容区 | 是 | I-beam | TextInput / Click / Drag / ESC / Click_Outside | S3 / S3 / S3 / S2 / S2 | 显示闪烁的文本光标；选中文本时蓝色背景 | Click移动光标；Drag选择文本；ESC退出编辑 |
| S3-Editing | P03-边框 | 是 | - | - | - | 显示为虚线 | 仅显示，不可交互 |
| S3-Editing | P04-缩放点(角) | 否 | - | - | - | 隐藏 | 编辑态隐藏控制点，防止误操作 |
| S3-Editing | P05-缩放点(边) | 否 | - | - | - | 隐藏 | 编辑态隐藏控制点 |
| S3-Editing | P06-旋转柄 | 否 | - | - | - | 隐藏 | 编辑态隐藏旋转柄 |
| | | | | | | | |
| **S4-Moving** | P01-填充区域 | 是 | Move | Dragging / MouseUp / ESC | S4 / S2 / S2 | 对象跟随鼠标移动；显示对齐辅助线 | 持续拖拽保持移动；释放鼠标或ESC完成 |
| S4-Moving | P02-文本内容区 | 是 | - | - | - | 随对象移动 | 文本随对象移动，但不响应交互 |
| S4-Moving | P03-边框 | 是 | - | - | - | 随对象移动 | 边框随对象移动 |
| S4-Moving | P04-缩放点(角) | 否 | - | - | - | 不显示 | 移动中隐藏控制点 |
| S4-Moving | P05-缩放点(边) | 否 | - | - | - | 不显示 | 移动中隐藏控制点 |
| S4-Moving | P06-旋转柄 | 否 | - | - | - | 不显示 | 移动中隐藏旋转柄 |
| | | | | | | | |
| **S5-Resizing** | P01-填充区域 | 是 | - | - | - | 随对象缩放 | 填充区域随对象缩放，但不响应交互 |
| S5-Resizing | P02-文本内容区 | 是 | - | - | - | 随对象缩放 | 文本随对象缩放 |
| S5-Resizing | P03-边框 | 是 | - | - | - | 随对象缩放 | 边框随对象缩放 |
| S5-Resizing | P04_TL-左上角点 | 是 | Resize (↖↘) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示（如300x200） | 持续拖拽缩放；释放或ESC完成 |
| S5-Resizing | P04_TR-右上角点 | 是 | Resize (↗↙) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示 | 持续拖拽缩放 |
| S5-Resizing | P04_BR-右下角点 | 是 | Resize (↖↘) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示 | 持续拖拽缩放 |
| S5-Resizing | P04_BL-左下角点 | 是 | Resize (↗↙) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示 | 持续拖拽缩放 |
| S5-Resizing | P05_T-上边点 | 是 | Resize (↕) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示 | 单向拉伸高度 |
| S5-Resizing | P05_R-右边点 | 是 | Resize (↔) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示 | 单向拉伸宽度 |
| S5-Resizing | P05_B-下边点 | 是 | Resize (↕) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示 | 单向拉伸高度 |
| S5-Resizing | P05_L-左边点 | 是 | Resize (↔) | Dragging / MouseUp / ESC | S5 / S2 / S2 | 实时显示尺寸提示 | 单向拉伸宽度 |
| S5-Resizing | P06-旋转柄 | 否 | - | - | - | 不显示 | 缩放中隐藏旋转柄 |
| | | | | | | | |
| **S6-Rotating** | P01-填充区域 | 是 | - | - | - | 随对象旋转 | 填充区域随对象旋转，但不响应交互 |
| S6-Rotating | P02-文本内容区 | 是 | - | - | - | 随对象旋转 | 文本随对象旋转 |
| S6-Rotating | P03-边框 | 是 | - | - | - | 随对象旋转 | 边框随对象旋转 |
| S6-Rotating | P04-缩放点(角) | 否 | - | - | - | 不显示 | 旋转中隐藏缩放点 |
| S6-Rotating | P05-缩放点(边) | 否 | - | - | - | 不显示 | 旋转中隐藏缩放点 |
| S6-Rotating | P06-旋转柄 | 是 | Rotate (🔄) | Dragging / MouseUp / ESC | S6 / S2 / S2 | 实时显示角度提示（如45°） | 持续拖拽旋转；释放或ESC完成 |

---

## 三、表格使用说明

### 3.1 Excel 格式建议

**列宽设置：**
- A列（状态源）：120px
- B列（属性）：150px
- C列（可见）：80px
- D列（鼠标）：130px
- E列（操作）：180px
- F列（目标）：120px
- G列（反馈）：200px
- H列（备注）：250px

**格式美化：**
1. **表头行**（第1行）：
   - 背景色：深蓝色（RGB: 68, 114, 196）
   - 字体：白色，加粗，12pt
   - 对齐：居中

2. **状态分组标题行**（如 S0-Idle, S1-Hovered）：
   - 背景色：浅灰色（RGB: 217, 217, 217）
   - 字体：加粗
   - 边框：上边框加粗

3. **数据行**：
   - 交替行颜色：白色 / 浅蓝色（RGB: 221, 235, 247）
   - 字体：常规，11pt
   - 对齐：左对齐

4. **"属性可见"列**：
   - "是"：绿色字体
   - "否"：灰色字体

5. **关键操作行**（如状态转换）：
   - "状态(目标)"列中有值的行，可以用浅黄色高亮背景

---

### 3.2 筛选和分组

**添加自动筛选：**
1. 选中表头行
2. 数据 → 筛选
3. 可以按"状态(源)"、"可交互属性"、"鼠标样式"等列进行筛选

**分组建议：**
- 按"状态(源)"分组：便于查看某个状态下的所有规则
- 按"可交互属性"分组：便于查看某个属性在所有状态下的行为

**使用场景示例：**
- **产品评审**：筛选 S2-Selected 状态，查看所有选中态的交互规则
- **开发实现**：筛选 P02-文本内容区，查看文本区在所有状态下的行为
- **测试用例**：筛选"状态(目标)"列不为空的行，生成测试场景

---

## 四、补充表格：状态转换汇总表

### 4.1 状态转换快速查询表

这是一个辅助表格，用于快速查询状态之间的转换关系：

| 从状态 | 到状态 | 触发条件 | 涉及属性 | 备注 |
|-------|-------|---------|---------|------|
| S0-Idle | S1-Hovered | MouseEnter | 任意属性 | 鼠标进入对象区域 |
| S1-Hovered | S0-Idle | MouseLeave | - | 鼠标离开对象区域 |
| S1-Hovered | S2-Selected | Click | P01-填充区 / P02-文本内容区 | 单击选中对象 |
| S1-Hovered | S3-Editing | DoubleClick | P02-文本内容区 | 双击文本直接进入编辑 |
| S1-Hovered | S4-Moving | Drag | P01-填充区 | 拖拽直接进入移动 |
| S2-Selected | S0-Idle | Click_Outside / ESC | - | 取消选中 |
| S2-Selected | S3-Editing | DoubleClick / F2 | P02-文本内容区 | 进入文本编辑 |
| S2-Selected | S4-Moving | Drag | P01-填充区 | 拖拽主体移动 |
| S2-Selected | S5-Resizing | Drag | P04/P05-缩放点 | 拖拽缩放点缩放 |
| S2-Selected | S6-Rotating | Drag | P06-旋转柄 | 拖拽旋转柄旋转 |
| S3-Editing | S2-Selected | ESC / Click_Outside | - | 退出编辑，返回选中态 |
| S4-Moving | S2-Selected | MouseUp / ESC | - | 完成移动或取消 |
| S5-Resizing | S2-Selected | MouseUp / ESC | - | 完成缩放或取消 |
| S6-Rotating | S2-Selected | MouseUp / ESC | - | 完成旋转或取消 |

**这个表格放在 Sheet 2，作为快速参考。**

---

## 五、补充表格：属性优先级表

### 5.1 属性检测优先级表

当鼠标位置同时命中多个属性时，按以下优先级响应：

| 优先级 | 属性类型 | 包含的属性 | 命中范围 | 说明 |
|-------|---------|----------|---------|------|
| **1 (最高)** | 控制点 | P04-缩放点(8个), P06-旋转柄 | 热区半径 6-8px | 优先响应控制点 |
| **2** | 特殊交互区 | P02-文本内容区 | 文本字符边界 | 精确检测文本区域 |
| **3** | 边界 | P03-边框 | 边框宽度 ±3px | 边框附近区域 |
| **4 (最低)** | 主体 | P01-填充区域 | 对象内部全部区域 | 兜底响应 |

**检测逻辑：**
```
伪代码：
if (state == S2-Selected) {
    if (鼠标距离任意控制点 <= 6px) {
        return 对应的控制点属性;
    }
}

if (鼠标在文本字符边界内) {
    return P02-文本内容区;
}

if (鼠标距离边框 <= 3px) {
    return P03-边框;
}

if (鼠标在对象边界内) {
    return P01-填充区域;
}

return null;
```

**这个表格放在 Sheet 3。**

---

## 六、Excel 工作簿结构建议

### 工作簿结构

```
文本框交互规则.xlsx
├─ Sheet 1: 【交互规则总表】
│    ↑ 主表格，包含所有 84 个单元格的详细规则
│
├─ Sheet 2: 【状态转换汇总】
│    ↑ 状态转换的快速查询表
│
├─ Sheet 3: 【属性优先级】
│    ↑ 属性检测的优先级规则
│
├─ Sheet 4: 【状态定义】
│    ↑ 7个状态的详细说明
│
└─ Sheet 5: 【属性定义】
     ↑ 12个属性的详细说明
```

---

### Sheet 4: 状态定义表

| 状态ID | 状态名称 | 中文名 | 描述 | 进入条件 | 退出条件 | 特征 |
|-------|---------|-------|------|---------|---------|------|
| S0 | Idle | 默认态 | 对象创建后未交互的初始状态 | 对象创建；取消选中 | 鼠标进入；点击对象 | 无控制点；无交互反馈 |
| S1 | Hovered | 悬停态 | 鼠标悬停在对象上但未点击 | 鼠标进入对象区域 | 鼠标离开；点击 | 光标变化；可选高亮 |
| S2 | Selected | 选中态 | 对象被选中，显示控制点 | 点击对象 | 点击外部；ESC | 显示8个缩放点和旋转柄 |
| S3 | Editing | 编辑态 | 正在编辑文本内容 | 双击文本；按F2 | ESC；点击外部 | 显示文本光标；隐藏控制点 |
| S4 | Moving | 移动中 | 正在拖拽移动对象 | 拖拽对象主体 | 释放鼠标；ESC | 对象跟随鼠标；对齐辅助线 |
| S5 | Resizing | 缩放中 | 正在缩放对象尺寸 | 拖拽缩放点 | 释放鼠标；ESC | 实时缩放；显示尺寸提示 |
| S6 | Rotating | 旋转中 | 正在旋转对象角度 | 拖拽旋转柄 | 释放鼠标；ESC | 实时旋转；显示角度提示 |

---

### Sheet 5: 属性定义表

| 属性ID | 属性名称 | 几何类型 | 描述 | 适用状态 | 检测范围 | 备注 |
|-------|---------|---------|------|---------|---------|------|
| P01 | 填充区域 | Region（区域） | 文本框的主体区域，不包括控制点 | 所有状态 | 对象边界内的全部区域 | 最低优先级，兜底响应 |
| P02 | 文本内容区 | Region | 包含文字的区域 | 所有状态 | 文本字符的边界框 | 精确检测文本边界 |
| P03 | 边框 | Line（线） | 文本框的外边框线 | 所有状态 | 边框宽度 ±3px | 行为通常与填充区相同 |
| P04_TL | 左上角缩放点 | Point（点） | 左上角的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↖↘ |
| P04_TR | 右上角缩放点 | Point | 右上角的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↗↙ |
| P04_BR | 右下角缩放点 | Point | 右下角的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↖↘ |
| P04_BL | 左下角缩放点 | Point | 左下角的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↗↙ |
| P05_T | 上边缩放点 | Point | 上边中点的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↕ 单向拉伸高度 |
| P05_R | 右边缩放点 | Point | 右边中点的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↔ 单向拉伸宽度 |
| P05_B | 下边缩放点 | Point | 下边中点的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↕ 单向拉伸高度 |
| P05_L | 左边缩放点 | Point | 左边中点的缩放控制点 | 仅S2, S5 | 热区半径 6px | 光标：↔ 单向拉伸宽度 |
| P06 | 旋转柄 | Point | 顶部的旋转控制手柄 | 仅S2, S6 | 热区半径 8px | 光标：🔄 旋转光标 |

---

## 七、使用技巧

### 7.1 产品需求文档（PRD）

**直接引用表格：**
```
在 PRD 中，可以直接引用：
"文本框在 S2-Selected 状态下，用户拖拽 P04_TL（左上角缩放点）时，
进入 S5-Resizing 状态，鼠标显示 ↖↘ 光标，详见《文本框交互规则.xlsx》
第 14 行。"
```

### 7.2 开发任务拆分

**按状态拆分：**
- 任务1：实现 S0-Idle 和 S1-Hovered（筛选这两个状态的所有行）
- 任务2：实现 S2-Selected（筛选这个状态的所有行）
- 任务3：实现 S3-Editing
- ...

**按属性拆分：**
- 任务1：实现 P01-填充区域 和 P02-文本内容区的所有交互
- 任务2：实现 P04/P05 缩放点的所有交互
- 任务3：实现 P06-旋转柄的所有交互

### 7.3 测试用例生成

**从表格生成测试用例：**
1. 筛选"状态(目标)"列不为空的行 → 这些是需要测试的状态转换
2. 每一行 = 一个测试场景
3. 示例测试用例：

```
测试用例 TC-001
前置条件：文本框处于 S1-Hovered 状态
测试步骤：鼠标悬停在 P02-文本内容区，执行 DoubleClick 操作
预期结果：
  - 鼠标样式：I-beam（工字型）
  - 状态转换：S1 → S3
  - 视觉反馈：显示闪烁的文本光标
```

---

## 八、总结

### 表格的核心价值

✅ **完备性**
- 84 个单元格，覆盖所有 (属性 × 状态) 组合
- 每个单元格都有明确定义

✅ **可读性**
- 结构清晰，易于阅读和查询
- 支持筛选、分组、搜索

✅ **可维护性**
- 新增属性或状态时，只需添加行
- 修改规则时，定位到具体单元格即可

✅ **跨团队协作**
- 产品、设计、开发、测试都能理解
- 统一的"交互语言"

---

**这个表格结构是否符合你的需求？有没有需要调整的地方？**
